# 数据库密码配置说明

## 🔍 您需要配置的密码

在 `application.yml` 中需要配置的密码是：**数据库用户 `tibet_user` 的密码**

**不是** MySQL root 密码！

## 📝 两种情况

### 情况1：如果之前已经创建了数据库用户

如果您在修复MySQL配置时已经创建了 `tibet_user` 用户，使用当时设置的密码。

### 情况2：如果还没有创建数据库用户

需要先创建数据库和用户，然后使用设置的密码。

## ✅ 创建数据库和用户的步骤

在服务器终端执行：

```bash
# 1. 登录MySQL（使用root密码）
mysql -u root -p
```

输入MySQL root密码：mBh7FriAwM2uybEqQGU8yJwxVGnw6H1N2dxP/ncaIBs=

```sql
# 2. 创建数据库
CREATE DATABASE IF NOT EXISTS tibet_tourism CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 3. 创建用户并设置密码（替换 your_password 为您想要的密码）
CREATE USER IF NOT EXISTS 'tibet_user'@'localhost' IDENTIFIED BY 'your_password';

# 4. 授权
GRANT ALL PRIVILEGES ON tibet_tourism.* TO 'tibet_user'@'localhost';

# 5. 刷新权限
FLUSH PRIVILEGES;

# 6. 退出
EXIT;
```

## ⚙️ 配置 application.yml

编辑配置文件：

```bash
cd /opt/colorful-tibet/backend
nano src/main/resources/application.yml
```

找到以下部分：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tibet_tourism?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: tibet_user
    password: 您的数据库密码  # ← 这里替换为您在上面设置的密码
```

**示例**（如果密码是 `tibet_password_123`）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tibet_tourism?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: tibet_user
    password: tibet_password_123  # 替换为实际密码
```

## 🔐 密码建议

- **长度**：至少8个字符
- **复杂度**：包含字母、数字
- **安全性**：不要使用过于简单的密码
- **示例**：`Tibet2024!Tourism` 或 `tibet_db_2024_pass`

## 📋 完整配置示例

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tibet_tourism?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: tibet_user
    password: Tibet2024!Tourism  # 您设置的密码
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
```

## ✅ 验证配置

配置完成后，可以测试连接：

```bash
# 测试数据库连接
mysql -u tibet_user -p tibet_tourism
# 输入您设置的密码，如果成功进入MySQL命令行，说明配置正确
```

## 🎯 重要提示

1. **保存好密码**：配置完成后，请妥善保存数据库密码
2. **不要泄露**：不要将密码提交到Git仓库
3. **生产环境**：建议使用环境变量或密钥管理服务

---

**总结**：
- MySQL root密码：`mBh7FriAwM2uybEqQGU8yJwxVGnw6H1N2dxP/ncaIBs=`（用于管理）
- 数据库用户密码：您自己设置的密码（用于应用连接）


