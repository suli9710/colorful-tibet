# Code Review Issues

Last updated: 2026-06-09
Last reviewed: 2026-06-09 18:15:00 +08:00

This file tracks issues re-checked against the current worktree. Items marked fixed have code and tests in this worktree; residual items remain candidates for the next 10-minute review cycle.

## Verification Snapshot

- Product-hardening multi-agent cycle on 2026-06-09 18:15 +08:00: 4 development agents and 1 review agent completed; the parallel testing agent was started but did not return before local full validation completed. Closed CT-FE-056, CT-FE-057, CT-BE-040, CT-BE-041, CT-OPS-030, and CT-OPS-031 in the current worktree. RouteCommunity filters now reset pagination and ignore stale route/Q&A responses; Admin hotel-order status changes now expose accessible row-level busy/error/rollback behavior; guide-chat usage keys use HMAC labels and production usage accounting fails closed when Redis is unavailable; production rate limiting and brute-force protection now require Redis and fail closed for sensitive paths when Redis is missing or unavailable; production Nginx rejects unmatched HTTPS hosts and validates rendered host variables; and production metrics default to non-public with preflight/static ops tests guarding the setting. Validation passed focused frontend suites for RouteCommunity and AdminDashboard hotel orders, focused backend security coverage, `npm run test:ops` (30 tests), full `npm run check` (backend compile/test, frontend typecheck plus 49 files / 304 tests plus production build, Scrapler 16 tests), and `git diff --check` with only LF-to-CRLF working-copy warnings.
- Product-hardening follow-up on 2026-06-09 17:18 +08:00: local follow-up closed the review agent's remaining high/medium findings plus stale test drift. `CACHE_KEY_HMAC_SECRET` is now an independent production-required secret with placeholder and length validation across backend, compose, upload, preflight, docs, and guardrails. Recommendation/AI Redis values no longer store raw similar-user/user identifiers. AI route quota and pending-start controls fail closed in production when Redis is unavailable. `QuestionDetail.vue` separates detail and answer failures, retries them independently, reloads on route-param changes, and ignores stale detail/answer/like-status responses. `UserProfile.vue` no longer reports a successful password change as failed by reloading stale session-gated profile details. AdminDashboard's remaining spot/news/carousel/route/hotel loaders preserve rows on transient failures and ignore stale refreshes. Validation passed focused frontend stale/UX coverage (4 files / 42 tests), `sourceGuardrails.test.ts` (19 tests), `opsConfigGuardrails.test.ts` (9 tests), focused backend AI/privacy/production-safety coverage, full `mvn -q -f backend/pom.xml test`, full `npm run check` (backend compile/test, frontend typecheck plus 49 files / 301 tests plus production build, Scrapler 16 tests), and `git diff --check` with only LF-to-CRLF working-copy warnings.
- Product-hardening multi-agent cycle on 2026-06-09 16:16 +08:00: 4 development agents, 1 testing agent, and 1 review agent completed. Closed CT-SEC-PII-002, CT-FE-050, and CT-FE-051, and also addressed review follow-ups for AI route/quota Redis key privacy plus AdminDashboard/AdminHeritage stale-response gaps. Redis/cache keys for IP geolocation, recommendation profiles, AI quota, and AI route cache/locks now use keyed HMAC labels instead of raw IP, user ID, or preference tuples. AdminDashboard user and hotel-order lists now preserve rows on failures, expose retryable error states, and ignore stale refresh responses. RouteCommunity route/question lists and AdminHeritagePanel relation/top-level item lists now ignore stale success/failure responses. UserProfile forced-password-change UX now has mounted DOM coverage. Validation passed focused backend privacy/AI coverage, focused frontend UX/stale suites (6 files / 76 tests), `npm run test:ops` (18 tests), full `npm run check` (backend compile/test, frontend typecheck plus 48 files / 287 tests plus production build, Scrapler 16 tests), and final `git diff --check` with only LF-to-CRLF warnings.
- Product-hardening multi-agent cycle on 2026-06-09 15:30 +08:00: 4 development agents, 1 testing agent, and 1 review agent completed. Closed CT-FE-048, CT-FE-049, CT-BE-038, and CT-OPS-028 in the current worktree. `Favorites.vue` now guards stale pagination responses and exposes accessible localized failure/retry states; third-party AMap/reCAPTCHA script reuse and external booking links now fail closed against untrusted origins/hosts; `/api/orders/my` summary rows no longer return contact PII; and the deploy preflight test is included in the root ops gate. Review found no high-confidence High issues and added CT-SEC-PII-002, CT-FE-050, and CT-FE-051 as current Medium residuals. Validation passed focused frontend security/UX suites (6 files / 42 tests), `npm run test:ops` (18 tests), focused order Maven coverage, full `npm run check` (backend compile/test, frontend typecheck plus 47 files / 275 tests plus production build, Scrapler 16 tests), and final `git diff --check` with only LF-to-CRLF warnings.
- Review follow-up on 2026-06-09 13:40 +08:00: the追加 review agent's high and medium findings are fixed. `PiiLegacyCiphertextStartupGuard` now runs under the same production-intent predicate as `ProductionSafetyValidator` instead of only under `@Profile("prod")`, with regression coverage for `APP_ENV=production` under a local Spring profile. Docker digest evidence validation now rejects bare records arrays and requires the resolver object schema with metadata. Validation passed `mvn -q -f backend/pom.xml '-Dtest=PiiLegacyCiphertextStartupGuardTest,ProductionSafetyValidatorTest' test`, `npm run test:ops` (15 tests), `npm run check:supply-chain-pins`, full `npm run check` (backend 1493 tests, frontend 42 files / 230 tests plus build, Scrapler 16 tests), and `git diff --check` with LF-to-CRLF warnings only. The earlier sandbox-only `@TempDir` AccessDenied failure was disproved by the successful non-sandbox full check.
- Supply-chain pin closure on 2026-06-09 04:54 +08:00: CT-OPS-008 is fixed in the current worktree. Dockerfile bases, Compose service images, and CI workflow Prometheus images are pinned with Docker Hub registry `sha256` digests, and `docker-digest-evidence.json` records source-matched resolver evidence for every required image reference. Validation passed `npm run check:supply-chain-pins`, `npm run test:ops`, `docker compose --env-file .env.example -f docker-compose.yml config --quiet`, and `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet`; Docker emitted only the non-failing local `C:\Users\Suli\.docker\config.json` access warning during compose parsing.
- Production-readiness multi-agent closure on 2026-06-09 00:50 +08:00: 4 development agents, 1 testing agent, and 1 review agent completed. Local follow-up removed the remaining targeted `RouteDetail.vue` / `RoutePlanner.vue` / `HotelBooking.vue` broad `any` usage, moved the remaining RoutePlanner and HotelBooking user-facing copy/formatting into i18n/locale-aware formatting, hardened `NGINX_REDIRECT_HOST` preflight to reject non-host values, and closed the AI cross-instance start-lock/readable-record short window with Redis pending metadata plus retryable detached SSE behavior. Validation passed `mvn -q -f backend/pom.xml test`, `npm --prefix frontend run check` (42 files / 221 tests / production build), `python -m pytest -q scrapler` (16 tests), workflow YAML parsing, both compose config checks with `.env.example`, `npm run test:ops`, focused ops/i18n/route-hotel Vitest coverage, and `git diff --check` with LF-to-CRLF warnings only. `npm run check:supply-chain-pins` intentionally fails closed until trusted external action SHAs, image digests, and pip hashes are supplied.
- Production-readiness verification on 2026-06-09 00:05 +08:00: addressed the review agent's post-fix findings by splitting multi-domain `NGINX_SERVER_NAME` from single-host `NGINX_REDIRECT_HOST`, adding production preflight and ops guardrail coverage, narrowing OrderCenter search copy/logic to summary fields that are actually present before detail load, and making RouteDetail like toggles use backend-authoritative `likeCount`. Full validation passed `mvn -q -f backend/pom.xml test`, `npm --prefix frontend run check` (42 files / 218 tests / production build), `python -m pytest -q scrapler` (16 tests), workflow YAML parsing, both compose config checks with `.env.example`, focused ops/route/community Vitest coverage, and `git diff --check` with LF-to-CRLF warnings only. A networked attempt to generate external supply-chain pins timed out without usable output, so CT-OPS-008 remains a release-blocking fail-closed gate until trusted SHA/digest/hash values are supplied.
- Multi-agent production-readiness continuation on 2026-06-08 23:48 +08:00: the testing agent reported full backend, frontend, Scrapler, compose, and diff gates passing before the latest local follow-up. Dev-A fixed CT-BE-004 with Redis Stream backed AI route job events for cross-instance RUNNING SSE recovery and focused AI route tests. The main thread fixed CT-FE-027 by returning viewer `liked` state in scenic spot comment page payloads through one batched repository query and removing frontend per-comment liked-status requests. Local follow-up passed `mvn -q -f backend/pom.xml -Dtest=CommentControllerDtoTest test`, `mvn -q -f backend/pom.xml '-Dtest=CommentControllerDtoTest,CommentControllerLikeIdempotencyTest' test`, `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/api/endpointRegistry.test.ts`, and `npm --prefix frontend run typecheck`. Dev-C added a fail-closed supply-chain pin gate for CT-OPS-008, but production release still needs operator-supplied GitHub Actions SHAs, image digests, and pip hashes before that gate can pass.
- Final multi-agent closure on 2026-06-08 23:19 +08:00: four development agents completed the remaining backend, frontend, and ops residuals. CT-BE-034, CT-BE-035, CT-BE-036, CT-FE-036, CT-FE-037, CT-FE-038, CT-FE-041, CT-OPS-022, CT-OPS-023, CT-OPS-024, and CT-OPS-025 are fixed in the current worktree. Local validation passed `mvn -q -f backend/pom.xml -Dtest="AiRouteRecordServiceTest,HotelBookingServiceTest,OrderCenterServiceTest" test`, `mvn -q -f backend/pom.xml -DskipTests compile`, `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/views/communityA11y.test.ts src/views/routeHotelA11y.test.ts src/utils/devProxyTarget.test.ts`, `npm --prefix frontend run test -- --run src/utils/opsConfigGuardrails.test.ts src/utils/devProxyTarget.test.ts`, `npm --prefix frontend run check` (41 files / 210 tests / build), production/local compose config checks, and `git diff --check` with LF-to-CRLF warnings only.
- Heartbeat recheck on 2026-06-08 22:59 +08:00: reread the current worktree and reconciled the handoff findings. CT-BE-032, CT-FE-039, and CT-FE-040 remain fixed in the current code; the stale proposed CT-BE-037 is not open because `CommunityPublicDtoTest` now asserts the compact route summary contract and the focused Maven selector for `CurrentUserApplicationServiceTest,CommunityPublicDtoTest` passed. Added CT-BE-036, CT-FE-041, and CT-OPS-025 as current residuals. Targeted validation passed `mvn -q -f backend/pom.xml -Dtest=CommunityPublicDtoTest test` and `mvn -q -f backend/pom.xml -Dtest=CurrentUserApplicationServiceTest,CommunityPublicDtoTest test`.
- Final reconciliation on 2026-06-08 22:53 +08:00: rechecked the latest testing/review-agent findings against the current worktree. CT-BE-032 is now fixed through `TravelQuestionSummaryRow` projection queries; CT-FE-039 is fixed by gating RouteDetail like-status requests to authenticated users and using `skipAuthRedirect`; CT-FE-040 is fixed by returning compact parent spot/route summaries from comment DTOs. Focused validation passed `mvn -q -f backend/pom.xml -Dtest="CurrentUserApplicationServiceTest,CommunityPublicDtoTest" test` and `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts` (13 tests).
- Maintenance recheck on 2026-06-08 22:35 +08:00: closed all four review-agent handles and both testing-agent handles after reconciling their returned findings against the current worktree. Current code evidence keeps CT-BE-030, CT-BE-033, CT-FE-031, CT-FE-032, CT-FE-034, CT-FE-035, CT-OPS-019, and CT-OPS-020 fixed; moved stale CT-FE-033 to fixed status because News now sends server-side `category` / `keyword` filters and backend contract coverage exists; kept CT-BE-004, CT-BE-032, CT-FE-009, CT-FE-011, CT-FE-027, CT-OPS-008, and CT-OPS-022 open; and added CT-BE-034, CT-BE-035, CT-FE-036 through CT-FE-040, CT-OPS-023, and CT-OPS-024 as current residuals. Agent validation from this cycle passed backend compile plus a focused 9-class / 52-test Maven suite, frontend typecheck plus 7 Vitest files / 47 tests, both compose config checks, and `git diff --check` with LF-to-CRLF warnings only.
- Final production-readiness follow-up on 2026-06-08 22:31 +08:00: local reconciliation fixed CT-BE-030, CT-BE-033, CT-FE-031, CT-FE-032, CT-FE-034, CT-FE-035, CT-OPS-019, and CT-OPS-020. Targeted validation passed `mvn -q -f backend/pom.xml -Dtest="SharedRouteServiceTest,TravelQAServiceTest,DataSeederTest,ProductionSafetyValidatorTest" test`, `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts` (12 tests), and Scrapling `python -m unittest test_scraper` through the bundled Python plus `scrapler/.venv/Lib/site-packages` (9 tests). Earlier in the same follow-up, full backend `mvn -q -f backend/pom.xml test`, full frontend `npm --prefix frontend run check` (40 files / 195 Vitest tests plus production build), workflow YAML parsing, both compose config checks, PowerShell parser checks, `cmd /c stop.bat --dry-run`, deployment archive dry-run, and `git diff --check` all passed; Docker reported only the non-failing user config access warning and Git reported only LF-to-CRLF working-copy warnings.
- Latest long-review maintenance cycle on 2026-06-08 22:16 +08:00: all four review agents and both testing agents returned, and their findings were reconciled against current files after several stale handoff/agent line references were disproved locally. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile`; the original PowerShell double-quoted focused `-Dtest` invocation matched 0 tests, then the same list passed with single-quoted `-Dtest=...` as 9 test classes / 108 tests. Frontend/OPS validation passed `npm --prefix frontend run typecheck`, `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/router/authRedirect.test.ts src/api/endpointRegistry.test.ts src/views/orderProfileA11y.test.ts src/utils/sourceGuardrails.test.ts src/views/routeHotelA11y.test.ts src/components/feedbackUx.test.ts` (7 files / 44 tests), both compose config checks, and `git diff --check` with LF-to-CRLF warnings only. Current code evidence moved CT-BE-031, CT-FE-028, and CT-OPS-021 to fixed status; kept CT-BE-004, CT-BE-030, CT-FE-009, CT-FE-011, CT-FE-027, CT-FE-031, CT-FE-032, CT-OPS-008, CT-OPS-019, and CT-OPS-020 open; and added CT-BE-032, CT-BE-033, CT-FE-033, CT-FE-034, CT-FE-035, and CT-OPS-022 as current residuals.
- Latest long-review maintenance cycle on 2026-06-08 21:59 +08:00: all four review agents and both testing agents returned, with the backend/frontend/cross-layer review agents restarted after earlier handles were lost during continuation. Backend validation passed in the testing agent with `mvn -q -f backend/pom.xml -DskipTests compile` and `mvn -q -f backend/pom.xml -Dtest="OrderCenterServiceTest,ItineraryServiceTest,SharedRouteServiceTest,RecommendationServiceTest,PriceUpdateServiceTest,ScenicSpotControllerPrivacyTest,AiRouteGenerationJobServiceTest" test` (7 test classes / 89 tests). Frontend/OPS validation passed in the testing agent with `npm --prefix frontend run typecheck`, `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/router/authRedirect.test.ts src/api/endpointRegistry.test.ts src/views/orderProfileA11y.test.ts src/utils/sourceGuardrails.test.ts` (5 files / 30 tests), both `.env.example` compose config checks, and `git diff --check` with LF-to-CRLF warnings only. Local reconciliation moved CT-BE-029, CT-FE-023, CT-FE-024, CT-FE-025, CT-FE-026, CT-FE-029, and CT-FE-030 to fixed status; kept CT-BE-004, CT-FE-009, CT-FE-011, CT-FE-027, CT-FE-028, and CT-OPS-008 open; and added CT-BE-030, CT-BE-031, CT-FE-031, CT-FE-032, CT-OPS-019, CT-OPS-020, and CT-OPS-021 as current residuals.
- Latest long-review maintenance cycle on 2026-06-08 21:33 +08:00: all four review agents and both testing agents returned, and their conclusions were reconciled with local code evidence. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile`, then a clean focused Maven suite covering the current backend changes passed with 17 Surefire reports / 152 tests, plus `ScenicSpotCurrentUserRecommendationTest` passed with 9 more tests. Local final backend follow-up also passed `mvn -q -f backend/pom.xml -Dtest="OrderCenterServiceTest,ItineraryServiceTest,SharedRouteServiceTest,RecommendationServiceTest,PriceUpdateServiceTest,ScenicSpotControllerPrivacyTest,AiRouteGenerationJobServiceTest" test`. Frontend validation passed `npm --prefix frontend run typecheck`; `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/router/authRedirect.test.ts src/api/endpointRegistry.test.ts src/views/orderProfileA11y.test.ts src/utils/sourceGuardrails.test.ts` passed with 5 files / 30 tests. Compose config checks for both local and production files passed in the testing agent, and `git diff --check` passed with LF-to-CRLF warnings only. This moved CT-BE-001, CT-BE-005, CT-BE-006, CT-BE-009, CT-BE-028, CT-FE-017, and CT-FE-018 to fixed status; narrowed CT-FE-023 to legacy spot bookings in `UserProfile`; kept CT-BE-004, CT-BE-029, CT-FE-009, CT-FE-011, CT-FE-024 through CT-FE-028, and CT-OPS-008 open; and added CT-FE-029 plus CT-FE-030 as new current residuals.
- OPS safety slice on 2026-06-08 21:18 +08:00: `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.common.security.antibot.RecaptchaServiceTest" "-Dmaven.compiler.testIncludes=**/RecaptchaServiceTest.java" test` passed, `mvn -q -f backend/pom.xml -DskipTests compile` passed, both `.env.example` compose config checks passed, workflow YAML parsing passed through the frontend `yaml` package, the local dependency-policy guard passed, and targeted scans found no `X-Forwarded-For $remote_addr`, reCAPTCHA query-param verification fields, or frontend `npm install` fallback in the touched files. Full Maven test execution without `testIncludes` is still blocked by the pre-existing dirty-tree `ItineraryControllerPaginationTest` generic mismatch. This moved CT-OPS-007 to fixed status and partially mitigated CT-OPS-008 with a low-risk lockfile-required guard.
- Latest long-review maintenance cycle on 2026-06-08 21:12 +08:00: all four review agents and both testing agents returned, and their valid findings were reconciled against the current worktree. Local follow-up passed `mvn -q -f backend/pom.xml -Dtest="HeritageControllerPublicReadAccessTest,HeritageServicePaginationTest,CommunityPublicDtoTest,SharedRouteControllerPaginationTest,FavoriteControllerPrivacyTest,RecaptchaServiceTest" test` and `npm --prefix frontend run test -- --run src/api/stream.test.ts src/views/communityPagination.test.ts src/router/authRedirect.test.ts src/api/endpointRegistry.test.ts` (4 files / 21 Vitest tests). Current code evidence moved CT-OPS-018 to fixed status because reCAPTCHA verification now uses a form-urlencoded POST body and has a URI/body-shape regression test. Review plus local evidence added CT-BE-029, CT-FE-025, CT-FE-026, CT-FE-027, and CT-FE-028 as current residuals; CT-BE-024, CT-BE-025, CT-BE-026, CT-FE-019, CT-FE-020, CT-FE-021, CT-FE-022, CT-OPS-016, and CT-OPS-017 remain fixed in the current worktree.
- Final multi-agent production-readiness gate on 2026-06-08 21:04 +08:00: the four development agents, one parallel test agent, and one parallel review agent all returned and were closed. Follow-up review findings were addressed locally: SSE clean EOF now leaves route generation interrupted/resumable instead of falsely complete, favorite duplicate-key races are idempotent, and public question lists use summary DTOs without full `content`. Full backend `mvn -q -f backend/pom.xml test` passed, full frontend `npm --prefix frontend run check` passed (typecheck, 40 Vitest files / 185 tests, and production build), local and production compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, workflow YAML parsing passed, PowerShell parser checks passed for deployment scripts, `cmd /c stop.bat --dry-run` passed, and the deployment archive dry-run passed with 798 Git-tree files approved. This moved CT-BE-024, CT-BE-025, CT-BE-026, CT-BE-027, CT-FE-019, CT-FE-020, CT-FE-021, CT-FE-022, CT-OPS-016, and CT-OPS-017 to fixed status.
- Latest long-review cycle on 2026-06-08 20:53 +08:00: all four review agents and both testing agents returned. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile` and the backend testing agent's 34-class / 224-test focused suite; local conflict resolution also passed `mvn -q -f backend/pom.xml -Dtest="SharedRouteControllerPaginationTest,TravelQAControllerPaginationTest,SharedRouteServiceTest,TravelQAServiceTest,ProductionSafetyValidatorTest,RecaptchaServiceTest" test`. Frontend/OPS validation passed a 14-file / 70-test focused Vitest suite, `npm --prefix frontend run typecheck`, `git diff --check`, and both compose config checks with `.env.example`. Review plus local evidence moved CT-BE-027, CT-FE-020, CT-FE-022, and CT-OPS-016 to fixed status, narrowed CT-BE-026 to the remaining favorite race, and added CT-BE-028, CT-FE-023, CT-FE-024, and CT-OPS-018 as current residuals.
- Final multi-agent production-readiness gate on 2026-06-08 20:37 +08:00: the four development agents, one parallel test agent, and one parallel review agent all returned and were closed. Full backend `mvn -q -f backend/pom.xml test` passed, full frontend `npm --prefix frontend run check` passed (typecheck, 38 Vitest files / 175 tests, and production build), local and production compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, the deployment archive dry-run passed with 793 Git-tree files approved, and workflow YAML parsing passed. Follow-up fixes moved CT-BE-021, CT-BE-022, CT-BE-023, CT-FE-015, CT-FE-016, and CT-OPS-015 to fixed status; the PII legacy guard now rejects placeholder legacy keys and verifies sample legacy ciphertext decryptability instead of trusting any nonblank key.
- Latest long-review cycle on 2026-06-08 20:31 +08:00: all four review agents and both testing agents returned. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile` and a focused 11-class / 87-test Maven suite covering order center, payment audit, PII crypto/backfill, recommendation limits, itinerary pagination, and companion inference. Frontend validation passed the 7-file / 30-test focused Vitest suite and `npm --prefix frontend run typecheck`. OPS validation reported `git diff --check`, both `.env.example` compose config checks, and PowerShell parser checks passing, with only LF-to-CRLF and stale browser data warnings. Review plus local evidence removed stale CT-BE-021/022/023 and CT-FE-015/016 open entries, kept CT-OPS-006 fixed, and added CT-BE-024 through CT-BE-027, CT-FE-019 through CT-FE-022, and CT-OPS-016/017 as current residuals.
- Final production-readiness gate on 2026-06-08 20:11 +08:00: full `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (typecheck, 37 Vitest files / 170 tests, and production build), both compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, and `scripts/New-DeploymentArchive.ps1` produced a deployment archive dry-run with 787 Git-tree files approved and no forbidden local uploads/generated artifacts.
- Follow-up recheck on 2026-06-08 20:05 +08:00: PowerShell parser checks passed for `upload-server.ps1` and `deploy-new-server-http.ps1`; the embedded remote bash scripts from both files passed `bash -n`; a temp-file bash harness passed for allowed project paths, rejected `..`/`.`/`//`/outside-prefix inputs, rejected project symlink escapes, and rejected `.env`/`data`/`logs`/`certs` symlinks. `docker compose --env-file .env.example -f docker-compose.yml config --quiet`, `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet`, and `git diff --check` passed apart from LF-to-CRLF warnings. The current `upload-server.ps1` and `deploy-new-server-http.ps1` reject non-normalized project paths, require `realpath`, re-check the created path with `realpath -e`, refuse preserved symlink paths, and use `cd -P` plus `find .` before destructive replacement, so CT-OPS-013 is fixed in the current worktree and no longer belongs in the open list.
- PII safety follow-up on 2026-06-08 20:09 +08:00: `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.common.security.PiiCryptoConverterTest,com.tibet.tourism.common.security.PiiBackfillRunnerTest,com.tibet.tourism.common.security.PiiLegacyCiphertextStartupGuardTest" test` passed, and `mvn -q -f backend/pom.xml -DskipTests compile` passed. `PiiCryptoConverter` now fails closed on encrypted-prefix decrypt failures instead of returning raw `enc:*` ciphertext, and the prod-only `PiiLegacyCiphertextStartupGuard` fails startup when legacy `enc:v1:%` rows remain without `PII_ENCRYPTION_KEY`.
- Latest long-review cycle on 2026-06-08 19:55 +08:00: all four review agents and both testing agents returned. Frontend testing passed `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. The backend testing agent initially hit a transient compile failure while `reserveRefundTransaction(...)` call sites were changing, but the current worktree now passes `mvn -q -f backend/pom.xml -DskipTests compile` and the focused backend suite covering order center, companion inference, recommendation limits, and PII backfill/crypto. Review agents revalidated all remaining BE/FE/OPS residuals, found CT-OPS-014 fixed in the current scripts/compose wiring, and added CT-BE-021, CT-BE-022, CT-BE-023, and CT-OPS-015 as new residuals.
- Final production-readiness sweep on 2026-06-08 19:47 +08:00: `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.OrderCenterControllerPrivacyTest" test` passed, full `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (37 Vitest files / 170 tests plus typecheck/build), `git diff --check` passed with LF-to-CRLF warnings only, both compose config checks with `.env.example` passed, and `scripts/New-DeploymentArchive.ps1` successfully produced a Git-tree deployment archive after new production/deployment files were added to the Git index. Final review follow-up fixed stale same-order/same-refund transaction reservation recovery, reservation order-delete cascade, and deployment archive expected-vs-actual content checks.
- Latest heartbeat cycle on 2026-06-08 19:37 +08:00: all four review agents and both testing agents returned. Frontend validation passed in the testing agent with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. Backend validation passed in the testing agent with 10 focused test classes / 71 tests plus `mvn -q -DskipTests compile`. Local follow-up passed `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.spot.application.CompanionInferenceServiceTest" test`, showing payment/refund transaction-number reservations are covered and companion-inference logs now render `36.00%` instead of literal `{:.2f}`. `git diff --check` passed with LF-to-CRLF warnings only after removing trailing whitespace in the companion-inference log line. Review agents revalidated the remaining backend, frontend, and OPS residuals; CT-BE-018, CT-BE-019, and CT-BE-020 now have current code/test evidence for fixed status.
- Latest continuation cycle on 2026-06-08 19:18 +08:00: all four review agents and both testing agents returned. Frontend validation passed with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. Backend validation passed two focused Maven suites covering orders, route/itinerary, PII, recommendation, companion inference, AI route, guide chat, price updates, current-user, controller privacy, and log guardrails (76 tests + 86 tests), plus `mvn -q -DskipTests compile`. Local follow-up also passed the recommendation/companion focused suite and confirmed production recommendation paths no longer call unbounded `historyRepository.findByUserId(...)`. Review agents confirmed CT-BE-014, CT-BE-016, and CT-BE-017 are fixed in the current worktree; revalidated frontend and OPS residuals; narrowed CT-BE-018 to cross-order payment transaction-number races; and added refund transaction-number and companion-inference log-format residuals.
- Latest heartbeat cycle: all four review agents and both testing agents returned by 2026-06-08 19:04 +08:00. Frontend focused tests passed locally with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts` (5 files / 18 tests) after the frontend test agent also reported that suite and `npm run typecheck` passing. Backend testing initially reported a `RecommendationServiceTest` testCompile failure from corrupted string literals, but the current worktree has repaired those lines and a local rerun of `mvn -q -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.dto.CreateOrderRequestValidationTest,com.tibet.tourism.modules.route.application.ItineraryServiceTest,com.tibet.tourism.modules.route.web.ItineraryControllerPaginationTest,com.tibet.tourism.common.security.PiiCryptoConverterTest,com.tibet.tourism.common.security.PiiBackfillRunnerTest,com.tibet.tourism.modules.recommendation.application.RecommendationServiceTest" test` passed. `git diff --check` passed with only LF-to-CRLF warnings. The reported repository-noise heredoc CI risk did not reproduce: parsing `.github/workflows/ci.yml` with the frontend `yaml` package showed the `EOF` at shell column 1, and `bash -n` on the parsed `run` block exited 0.
- Latest production-readiness gates on 2026-06-08 19:15 +08:00: `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (37 Vitest files / 170 tests plus typecheck/build), `git diff --check` passed with LF-to-CRLF warnings only, and both `docker compose --env-file .env.example -f docker-compose.yml config --quiet` plus `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` passed. Bare compose config without env still fails on required secret variables, which is expected for production safety.
- Backend: `mvn -q -DskipTests compile`, focused security/auth/user tests, and latest full `mvn -q -f backend/pom.xml test` passed again on 2026-06-08 18:45 +08:00.
- Frontend: latest full `npm --prefix frontend run check` passed on 2026-06-08 18:45 +08:00 (`vue-tsc`, 37 Vitest files / 170 tests, and production build).
- Latest focused follow-up: `npm run test -- --run src/utils/devProxyTarget.test.ts` passed on 2026-06-08 16:55 +08:00 (1 file / 3 tests). `cmd /c stop.bat --dry-run` passed on 2026-06-08 16:57 +08:00 and resolved the project to `/mnt/c/Users/Suli/Desktop/colorful-tibet`. `npm run test -- --run src/utils/scriptSecurity.test.ts src/utils/amap.test.ts src/utils/recaptcha.test.ts` passed on 2026-06-08 17:00 +08:00 (3 files / 8 tests). `npm run test -- --run src/api/stream.test.ts` passed on 2026-06-08 18:01 +08:00 (1 file / 9 tests). `npm run test -- --run src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts` passed on 2026-06-08 18:05 +08:00 (2 files / 8 tests). `mvn -q -Dtest="com.tibet.tourism.common.security.*Test" test` passed on 2026-06-08 18:09 +08:00. `npm run test -- --run src/views/routeHotelA11y.test.ts` passed on 2026-06-08 18:11 +08:00 (1 file / 7 tests) and again on 2026-06-08 18:25 +08:00. `npm run test -- --run src/components/mobileNavigationA11y.test.ts` passed again on 2026-06-08 18:14 +08:00 (1 file / 5 tests).
- Compose preflight check: `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` and `docker compose --env-file .env.example -f docker-compose.yml config --quiet` exited 0 on 2026-06-08 17:35 +08:00 after the Redis/MySQL healthcheck secret handling update. A focused scan for `redis-server ... --requirepass`, `redis-cli -a`, and `mysqladmin ... -p` returned no matches in either Compose file.
- Negative compose preflight for CT-OPS-009: with all other `.env.example` values loaded and `SUPER_ADMIN_USERNAME` omitted, `docker compose -f docker-compose.prod.yml config --quiet` now exits 1 on 2026-06-08 18:02 +08:00 with the required-variable error. Positive production compose config still exits 0 with `.env.example`.
- Earlier testing agents completed: backend `mvn -q test` passed with 95 Surefire XML files / 796 tests; frontend `npm run check` passed with 35 Vitest files / 164 tests and production build.
- Earlier 4 code-review agents returned by 2026-06-08 18:48 +08:00. They confirmed CT-BE-011/012/013, CT-FE-012/013, and CT-OPS-004/010/011/012 no longer match their original open evidence; revalidated CT-OPS-006 as only partially mitigated; and added CT-BE-016/017, CT-FE-015/016/017/018, and CT-OPS-013/014.
- Diff hygiene: `git diff --check` passed on 2026-06-08 18:53 +08:00; it only reported LF-to-CRLF warnings.
- OPS follow-up: `.github/workflows/ci.yml` parsed with `yaml@2.6.1`, `python -m yamllint -d "{extends: relaxed, rules: {line-length: disable, new-lines: disable, truthy: disable}}" .github/workflows/ci.yml` passed, and actionlint v1.7.8 passed on 2026-06-08 18:43 +08:00. PowerShell parser checks passed for `upload-server.ps1`, `deploy-new-server-http.ps1`, and `scripts/New-DeploymentArchive.ps1`. A deployment archive dry-run passed with 771 Git-tree files approved and no forbidden local uploads/generated artifacts. `docker compose --env-file .env.example -f docker-compose.yml config --quiet` and `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` passed again.
- Latest focused follow-up for this review: `npm --prefix frontend run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` passed on 2026-06-08 18:44 +08:00 (7 files / 30 tests). `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.common.security.*Test,com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.BookingControllerAnonymousOwnerTest,com.tibet.tourism.modules.order.web.dto.CreateOrderRequestValidationTest" test` passed on 2026-06-08 18:44 +08:00.
- Known non-failing warnings: stale Browserslist/baseline data, Rolldown pure annotation warnings in `@vueuse/core`, and expected negative-path backend logs.

## Fixed This Cycle

- CT-FE-056: `RouteCommunity` now resets route/question pagination when filters, tags, or sort change after browsing later pages, and ignores stale route/Q&A responses.
- CT-FE-057: Admin hotel-order status changes now have accessible labels, row-level pending state, failure messages, and visible rollback to the original status on API failure.
- CT-BE-040: Guide-chat usage accounting now uses HMAC labels for quota identity keys and fails closed in production when the Redis usage backend is missing or unavailable.
- CT-BE-041: Production rate limiting and login brute-force protection now require Redis and fail closed for sensitive paths when Redis is missing or unavailable instead of silently using per-instance memory fallback.
- CT-OPS-030: Production Nginx now fails closed for unmatched HTTPS Host/SNI and validates runtime host interpolation values before rendering templates.
- CT-OPS-031: Production metrics now default to non-public, with deploy preflight and static ops tests rejecting anonymous metrics and Redis protection fallback defaults.
- CT-SEC-001: Authenticated `/api/guide/chat` now requires signed double-submit XSRF when an `AUTH_TOKEN` cookie is present. Anonymous public POST behavior remains allowed with browser metadata checks.
- CT-SEC-002: Controller authorization matrix now discovers real module `@RestController` classes automatically and covers previously omitted controllers.
- CT-SEC-003: CSRF trusted origins no longer accept wildcard patterns that CORS rejects when credentials are enabled.
- Private uploads: `/uploads/private/**` is excluded from public reads and denied for authenticated users too.
- CT-FE-001: Admin 401 responses now use centralized auth expiration unless a request explicitly sets `skipAuthRedirect`.
- CT-FE-002: Reported hardcoded endpoint paths were moved to `frontend/src/api/endpoints.ts` / auth modules, including follow-up profile/community/route share paths.
- CT-FE-003: Public rich text now strips remote HTTP(S) images while preserving local, same-origin, and bounded safe data images.
- CT-FE-004: SSE parsing now enforces buffer/frame byte limits and cancels hostile streams on overflow.
- CT-BE-002: Redis guide-chat window-limit rejections no longer consume daily quota; race compensation was added.
- CT-BE-003: Duplicate AI route job creation now has single-JVM serialization plus Redis start locks with TTL for cross-instance duplicate starts.
- CT-BE-004: AI route job query/stream now falls back to persisted RUNNING records when memory state is missing, startup recovery marks stale RUNNING records failed, and cross-instance RUNNING streams can relay Redis Stream job events instead of being limited to the in-memory subscriber list.
- CT-BE-005: Itinerary expansion now uses Hibernate batch fetching on days/items and related spot/hotel/room type references to reduce N+1 pressure.
- CT-BE-001: Scrapling prices are no longer hardcoded reference-only when upstream explicitly marks a result as publishable/non-reference; default behavior remains conservative.
- CT-BE-007: Batch price updates now delegate single-spot work to `SingleSpotPriceUpdateService` with `REQUIRES_NEW`, including async batch paths.
- CT-BE-008: Price update focused tests were updated for constructor injection and now compile/pass.
- CT-FE-005: Live2D rendering is gated behind desktop interaction or idle loading, with a lightweight placeholder before the model loads.
- CT-FE-006: Admin dashboard/community/heritage clickable panels now expose keyboard semantics with `role`, `tabindex`, accessible labels, and Enter/Space handlers; action controls use real buttons.
- CT-FE-007: Heat map remote geo JSON fallback is now disabled by default and gated behind `VITE_HEATMAP_REMOTE_GEO_FALLBACK_ENABLED`; local geo JSON remains the default path.
- CT-FE-008: AMap and reCAPTCHA dynamic loaders now apply shared third-party script security attributes, including CSP nonce, optional SRI, and `crossOrigin`, with env/meta/runtime nonce support covered by focused tests.
- CT-FE-010: AI route stream/job APIs now use `endpoints.routes.generateStream`, `generateJob`, `generateJobDetail(jobId)`, and `generateJobStream(jobId)` from the endpoint registry; `frontend/src/api/stream.test.ts` covers the generated fetch URLs.
- CT-OPS-001: Vite dev proxy target now derives from remote `VITE_API_BASE_URL`/`VITE_DEV_PROXY_TARGET` through `resolveDevProxyTarget`, so `frontend/start-remote.bat` no longer silently falls back to local `localhost:8080` for `/api` during remote debugging.
- CT-OPS-002: Production Compose now keeps `PUBLIC_METRICS_ENABLED=false` by default; bundled Prometheus requires an authenticated, allowlisted, or otherwise protected scrape path before anonymous metrics are enabled.
- CT-OPS-003: Production Compose no longer exposes Redis/MySQL credentials through the old long-lived `redis-server --requirepass`, `redis-cli -a`, or `mysqladmin -p...` command arguments; Redis now boots from a restricted temporary config file with an escaped `requirepass`, Redis healthcheck uses `REDISCLI_AUTH`, and local/production MySQL healthchecks use a temporary `--defaults-extra-file`.
- CT-OPS-005: `stop.bat` now uses Windows `>nul`, derives the project path from `%~dp0`, detects/overrides the WSL distribution, and supports `--dry-run`.
- CT-OPS-009: Production Compose now requires `SUPER_ADMIN_USERNAME` during compose interpolation, so missing super-admin configuration fails during preflight instead of later at backend startup.
- CT-OPS-010: Deployment archives are now produced through `scripts/New-DeploymentArchive.ps1`, which archives a temporary Git tree instead of enumerating the working directory, fail-fast rejects untracked production/deployment files that would be omitted, excludes untracked/ignored artifacts by construction, rejects forbidden deployment paths such as `output`, `data`, `logs`, `certs`, `backend/uploads`, `backend/target`, `frontend/dist`, and virtualenv/cache paths, and performs pre-upload tar contents checks including entry count plus exact ASCII-path expected-vs-actual comparison before either deployment script uploads.
- CT-OPS-011: The HTTP demo deployment no longer executes `https://get.docker.com` by default. Missing Docker now fails with preinstall guidance; convenience-script bootstrap requires explicit `-BootstrapDocker`, a 64-character `-DockerInstallScriptSha256`, and a pinned `-DockerInstallVersion`, then verifies SHA256 before running with `VERSION=...`.
- CT-OPS-012: CI now includes a workflow lint job for actionlint/yamllint, and the frontend Docker image build/scan passes deterministic production-like Vite build args for API base, reCAPTCHA, and AMap.
- CT-OPS-014: HTTP demo deploy now requires explicit `-DemoOnly`, writes `FRONTEND_HOST_BIND=0.0.0.0` alongside `FRONTEND_HOST_PORT=80`, uses the compose bind override instead of the loopback default, and fails when a public `SiteUrl` check fails.
- CT-OPS-013: Remote deployment project paths now reject `..`, `.`, repeated slash, and other non-normalized inputs; both deployment scripts require `realpath`, normalize `$PROJECT_DIR` under `/opt/colorful-tibet`, re-check the created directory with `realpath -e`, and use `cd -P` before destructive replacement so symlink/physical-path escapes are refused.
- CT-OPS-004: Deployment scripts no longer make `data` and `logs` world-writable. They derive `BACKEND_APP_UID/GID` from `.env` with safe defaults, `chown` those paths to the backend container user, and set directories/files to `750`/`640`.
- CT-OPS-006: PII decryption failures now emit low-cardinality `pii_decrypt_failure_total` metrics and structured security logs without raw ciphertext/PII. `PiiCryptoConverter` fails closed on encrypted-prefix decrypt failures instead of returning raw `enc:*` ciphertext to entity/response mapping, rejects placeholder legacy keys, and the prod-only `PiiLegacyCiphertextStartupGuard` scans legacy `enc:v1:%` rows even when a legacy key is configured so wrong keys fail startup through sample decryptability checks. `PiiBackfillRunner` skips undecryptable encrypted values instead of wrapping them into new v2 ciphertext, deployment validation rejects placeholder `PII_ENCRYPTION_KEY`, and Prometheus has a PII decrypt failure alert.
- CT-BE-009: `/api/orders/my` now uses sanitized `Pageable` with default size 20 and max size 50 while keeping the response body as a compatibility array plus pagination headers.
- CT-BE-010: Refund review/completion now has a production admin path. `OrderCenterController.java` exposes `/api/admin/orders/{orderId}/refunds/{refundId}/review`; `OrderCenterService` supports approve/reject/complete transitions, provider refund transaction-number conflict checks, rejected-cancellation state restoration, and voucher/inventory release on completed refunds; `OrderCenterServiceTest` covers approval, completion, rejection, duplicate provider refund transaction numbers, and partial cancellation refunds closing entitlements.
- CT-BE-014: Recommendation history reads now use bounded recent queries across the recommendation path and companion inference. `UserVisitHistoryRepository` centralizes recent/top-N queries, recommendation strategies use the bounded methods, and `CompanionInferenceService` uses recent visit history plus a paged recent booking sample.
- CT-BE-016: Late successful payment callbacks against closed or no-longer-payable orders no longer create successful `PaymentTransaction` rows. The service records a rejected callback audit and returns the current order state without reviving or polluting the order ledger.
- CT-BE-017: Order creation now catches idempotency-key unique constraint races, re-reads the existing order by user/idempotency key, and avoids creating duplicate inventory locks.
- CT-BE-018: Cross-order payment callback transaction-number races now claim the provider transaction number through `PaymentTransactionReservationService` before attaching a new payment transaction. `V20__reserve_payment_transaction_numbers.sql` adds a primary-key-backed reservation table with historical transaction backfill, conflicts re-read existing transactions/reservations, same-order stale reservations can recover when payload matches, and `OrderCenterServiceTest` covers both another-order races and same-order retry recovery.
- CT-BE-019: Refund provider transaction numbers now use the same atomic reservation path before refund state mutation. Explicit and generated refund transaction numbers reserve before completion, conflict checks include reservations, same-refund stale reservations can recover by `refundNo`, the reservation FK cascades when orders are deleted, and `OrderCenterServiceTest` covers both concurrent provider refund transaction-number claims and same-refund retry recovery.
- CT-BE-020: Companion inference success logs now pre-format confidence percentages with `Locale.ROOT` and use normal SLF4J `{}` placeholders, removing the literal `{:.2f}` output in logs.
- CT-BE-011: Profile stats now use count queries instead of loading complete route/booking histories, and `/api/auth/me/comments` now pages spot and route comments with page metadata.
- CT-BE-012: `CreateOrderRequest.items` now has `@Size(max = 20)` and service-level validation rejects empty or oversized item lists before repository lookups.
- CT-BE-013: Legacy `/api/bookings/my` now uses sanitized paging with default size 20 and max size 50 while preserving the array body and returning pagination headers.
- CT-BE-015: Security WebMvc slice tests no longer fail on missing access-denied-handler wiring; the focused `com.tibet.tourism.common.security.*Test` suite passes in the current worktree.
- CT-FE-012: `auth/me` and scenic spot comment like/liked calls now use endpoint registry helpers, with endpoint/auth tests covering the generated URLs.
- CT-FE-013: OrderCenter order cards now have keyboard semantics, and saved routes in UserProfile use real `router-link` navigation instead of a mouse-only heading.
- CT-FE-014: Footer About Us no longer uses a fake `href="#"`; it is now a keyboard-focusable button that opens the contact modal, and `frontend/src/components/mobileNavigationA11y.test.ts` asserts the footer has no fake share/about links.
- CT-BE-021: Public shared-route comments and travel-question answers are now paged with bounded/sanitized `Pageable` and pagination response metadata/headers.
- CT-BE-022: Saved AI route history now uses paged summary DTOs for `/api/routes/ai/saved`, with full generated content fetched only from the detail endpoint.
- CT-BE-023: `/api/routes/my-routes` now uses bounded paging and `SharedRouteSummaryResponse`, so current-user route lists no longer return every row or full route content.
- CT-FE-015: The broad substring-based 401 redirect exemptions were removed; only explicit `skipAuthRedirect` bypasses auth expiration, and protected booking-cancel/route-share 401 cases are covered by `frontend/src/api/client.test.ts`.
- CT-FE-016: `ContactModal` now has dialog semantics, labelled controls, focus management, and Escape/Tab keyboard handling.
- CT-OPS-015: CI and `scripts/New-DeploymentArchive.ps1` now reject suspicious tracked root shell artifacts such as `%s`, quote-prefixed fragments, semicolon leftovers, and control-token debris before they can be treated as approved deployable Git-tree entries.
- CT-BE-027: Route/question like handlers now return `LikeResult` values from the like/unlike service paths and read like counts through no-view-increment count lookups, so like/unlike no longer calls the detail loaders that increment `viewCount`. Covered by the current focused community tests.
- CT-BE-024: Public shared-route list responses now use `SharedRouteSummaryResponse`, so anonymous list browsing no longer transfers full route `content`; detail responses still return full content.
- CT-BE-025: Public heritage inheritor and event subresources now return capped `PageResponse` payloads with stable sorting instead of unbounded lists.
- CT-BE-026: Favorite creation now uses flush-time duplicate handling and returns the current favorite state/count idempotently under concurrent duplicate requests.
- CT-FE-020: Route comments, question answers, and profile shared routes now use `readPaginatedResponse`, preserve page metadata, and render load-more controls instead of silently showing only the first backend page.
- CT-FE-022: The route guard now preserves protected/admin `to.fullPath` through `createLoginRedirect`, and login consumes a same-origin sanitized redirect before falling back by role; `frontend/src/router/authRedirect.test.ts` covers external, looping, and control-character redirects.
- CT-OPS-016: Production reCAPTCHA defaults are no longer overridden off by `.env.example`; the example sets backend, frontend, and registration-required flags to true, and `ProductionSafetyValidator` rejects production deployments with reCAPTCHA disabled or placeholder site/secret keys.
- CT-FE-019: AI route job streaming now reports clean EOF without a terminal event as interrupted, preserves partial content/job id, clears loading state, and enables resume instead of leaving the route planner stuck.
- CT-FE-021: Scenic spot liked-state checks now run through bounded parallel/all-settled loading instead of serial per-comment awaits, reducing comment-page latency and isolating partial failures.
- CT-OPS-017: Local WSL helper commands now quote project paths through a bash-safe single-quote encoder in both `start.ps1` and `stop.bat`.
- CT-OPS-018: reCAPTCHA verification now sends `secret`, `response`, and `remoteip` as an `application/x-www-form-urlencoded` POST body instead of URL query parameters, and `RecaptchaServiceTest` asserts the outbound path has no raw query while the form body contains the verification fields.
- CT-OPS-007: `frontend/nginx.conf` and the HTTP-only `frontend/nginx.http.conf` now append forwarding chains with `$proxy_add_x_forwarded_for` instead of overwriting `X-Forwarded-For` with `$remote_addr`, with inline `real_ip_header` / `set_real_ip_from` guidance for deployments behind trusted upstreams.
- CT-BE-001: Reference-only or low-confidence fetched prices are now saved as durable `spot_price_observations` with `REVIEW_REQUIRED` status instead of being discarded, while still avoiding automatic publication to `ScenicSpot` prices.
- CT-BE-005: `/api/itineraries/my` now returns `ItinerarySummaryResponse` list rows without expanding days/items or related spot/hotel/room-type detail graphs.
- CT-BE-006: Authenticated scenic spot detail views now create/update `UserVisitHistory` and invalidate the user's recommendation cache, matching the current recommendation inputs that read visit history.
- CT-BE-009: `/api/orders/my` now returns `OrderSummaryResponse` rows without expanding items, payment transactions, refunds, vouchers, or invoices in the list path.
- CT-BE-028: Author shared-route deletion now removes route likes and comments before deleting the parent route, with service-order test coverage.
- CT-BE-029: Shared-route list paths now use `SharedRouteSummaryRow` projection queries through `SharedRouteRepository.findSummaries(...)` / `findSummariesByAuthorId(...)`, selecting list fields and author values without hydrating `SharedRoute.content` or dereferencing lazy author entities.
- CT-FE-017: `UserProfile` tabs now use roving `tabindex`, Arrow/Home/End keyboard handling, `aria-controls`, and labelled `tabpanel` containers.
- CT-FE-018: Admin carousel, route, hotel, and room-type loaders now report sanitized load failures through shared logging/toast handling instead of silently rendering empty lists.
- CT-FE-024: `ImageUploadField` no longer has a hardcoded `/admin/upload-image` default; `uploadEndpoint` is now a required prop and `frontend/src/utils/sourceGuardrails.test.ts` asserts the component cannot silently bypass the endpoint registry.
- CT-FE-023: Legacy scenic spot bookings in `UserProfile` now preserve `PageMetadata`, request `page`/`size`, append via `readPaginatedResponse`, and render a next-page control.
- CT-FE-025: Scenic spot comments now use `readPaginatedResponse`, request `page`/`size`, append subsequent pages, and expose a load-more control while hydrating liked state only for the newly visible page.
- CT-FE-026: Heritage inheritor/event consumers now normalize paged `{ content }` responses through `responseContent(...)` for featured and detail views.
- CT-FE-029: `OrderCenter` now normalizes summary rows with optional child arrays and fetches `/api/orders/{id}` for selected order details instead of assuming `/api/orders/my` contains full child collections.
- CT-FE-030: News and scenic spot browsing now preserve `PageResponse` metadata, send `page`/`size`, and expose next-page controls instead of limiting route-level browsing to backend page 0.
- CT-BE-031: Scenic spot view recording now uses `UserVisitHistoryRepository.upsertSpotView(...)` with MySQL `INSERT ... ON DUPLICATE KEY UPDATE`, so duplicate first-view races no longer retry inside the same rollback-prone persistence context.
- CT-FE-028: Travel question cards now consume a bounded `excerpt` from `TravelQuestionSummaryResponse` instead of the removed full `content` field, and `CommunityPublicDtoTest` asserts list responses include `excerpt` while omitting `content`.
- CT-OPS-021: The production nginx template now has an HTTP `default_server` that returns `444` for unknown hosts and the named HTTP redirect uses `https://${NGINX_SERVER_NAME}$request_uri` instead of `$host`.
- CT-BE-030: `DataSeeder.seedHistory()` now caps generated rows to unique `(user, spot)` pairs, tracks generated pairs before batching, and has `DataSeederTest` coverage for tiny user/spot sample spaces.
- CT-BE-033: Route and question like creation now use repository-level MySQL `INSERT IGNORE` methods and only increment counts when a row is actually inserted, avoiding rollback-only flush exception retries under duplicate first-like races.
- CT-FE-031: Heritage detail comments now request `page`/`size`, preserve `PageMetadata`, render page state and a load-more control, and append later comment pages with `mergeUniqueById`.
- CT-FE-032: News load-more requests now track the active append request token, so stale completions cannot strand `loadingMoreNews`; `communityPagination.test.ts` covers the stale search race.
- CT-FE-033: News browsing now sends selected `category` and normalized `keyword` to the backend list endpoint, backend news lists support those filters, and controller/frontend pagination tests cover the filtered contract.
- CT-FE-034: Scenic spot load-more requests now mirror the News token handling for `loadingMoreSpots`; `communityPagination.test.ts` covers stale search while a page append is in flight.
- CT-FE-035: The News empty/filter-no-results branch now renders the same load-more error alert as the non-empty list branch, so append failures remain visible.
- CT-OPS-019: `scrape_price_for_spot(...)` now passes `active_config.allowed_domains` into `validate_target_url(...)`; Scrapling tests prove off-allowlist candidates are skipped.
- CT-OPS-020: `ProductionSafetyValidator` now rejects blank or placeholder Scrapling API keys using the same placeholder detection used for reCAPTCHA keys, with a regression test for the `.env.example` placeholder.
- CT-BE-032: Travel question list paths now use `TravelQuestionSummaryRow` projection queries with database-side bounded excerpts and public author fields, and `TravelQAController` returns `PageResponse<TravelQuestionSummaryResponse>` without hydrating full `TravelQuestion.content` rows.
- CT-FE-039: Public shared route detail now skips `like-status` entirely for anonymous readers and uses `{ skipAuthRedirect: true }` for authenticated best-effort liked-state calls; `communityPagination.test.ts` covers both the no-guest-call path and the authenticated suppress-redirect option.
- CT-FE-040: Current-user comment DTOs now include compact parent summaries (`CommentDTO.spot.id/name` and `RouteCommentResponse.route.id/title`) while still omitting nested full entities and sensitive user fields; `CurrentUserApplicationServiceTest` and `CommunityPublicDtoTest` cover the contract.
- CT-BE-034: Heritage and scenic-comment likes now use repository-level MySQL `INSERT IGNORE` and only increment counts when a like row is actually inserted, avoiding rollback-only duplicate-key retries; idempotency tests cover both controllers.
- CT-BE-035: Hotel pending-booking expiry, platform pending-order expiry, and inventory-lock expiry now sweep fixed-size first-page batches, so stale backlogs are drained without one unbounded materialization or offset skip risk.
- CT-BE-036: AI route stale RUNNING startup recovery now processes fixed-size batches through a paged repository method; `AiRouteRecordServiceTest` covers recovery above one batch.
- CT-FE-036: `RouteCommunity` tracks route/question load errors separately from empty results and renders visible retry actions instead of showing empty states on API failure.
- CT-FE-037: `RouteCommunity` loading states now expose `role="status"`, `aria-live`, `aria-busy`, and screen-reader text, covered by `communityA11y.test.ts`.
- CT-FE-038: Scenic spot search now sends `keyword` plus optional `category` to the backend search endpoint, and backend search covers name, description, location, category, and tags instead of filtering only a page-local name result.
- CT-FE-041: `RouteDetail` now distinguishes not-found from transient load failures, renders retry, and isolates comment load errors so a comment failure does not erase a valid route detail.
- CT-OPS-022: Production examples no longer ship `change-me` infrastructure passwords, and production compose/upload preflight rejects placeholder DB, Redis, Grafana, and MySQL root credentials before services start.
- CT-OPS-023: Production proxy trust now defaults off with empty trusted CIDRs, and preflight rejects broad private CIDR trust so forwarded client IPs require explicit operator configuration.
- CT-OPS-024: The production Scrapling service is hardened with no-new-privileges, dropped capabilities, read-only filesystem, and writable `/tmp` tmpfs; the Dockerfile sets bytecode/home defaults compatible with read-only runtime.
- CT-OPS-025: `frontend/start-remote.bat` now requires an operator-provided backend URL and refuses non-localhost plaintext HTTP; `devProxyTarget` tests reject public HTTP proxy targets.
- Frontend rich-text XSS regression: `frontend/src/views/richTextXss.test.ts` now covers hostile Markdown/HTML payloads such as `javascript:`, event handlers, SVG, iframe, and unsafe data images through real DOM insertion assertions.
- CT-FE-027: Scenic spot comments now include viewer `liked` state in the paginated comment response using one batched `CommentLikeRepository.findLikedCommentIds(...)` lookup per page; `ScenicSpotDetail.vue` no longer calls `/comments/{id}/liked` for every visible comment, and frontend/backend tests assert the no-N+1 contract.
- CT-OPS-026: Production frontend nginx now supports multi-domain `server_name` without using that multi-value variable as the redirect host. `NGINX_REDIRECT_HOST` is required in production, must be a single host, is used for HTTP-to-HTTPS redirects, and is covered by compose preflight plus ops guardrail tests.
- CT-FE-042: RouteDetail like toggles now apply backend-authoritative `{ liked, likeCount }` responses instead of local increment/decrement drift, with Vitest coverage for non-local `likeCount` responses.
- CT-FE-043: OrderCenter search placeholder and summary-list search now match the data available in `OrderSummaryResponse`, so users are no longer promised voucher/item search before detail data is loaded.
- CT-BE-004 follow-up: AI route start-lock recovery now persists Redis pending metadata before quota/record creation, so other instances can verify ownership and emit retryable detached SSE when the RUNNING record is not yet readable instead of returning a snapshot that streams as 404.
- CT-OPS-026 follow-up: Production preflight and upload checks now require `NGINX_REDIRECT_HOST` to be a single canonical host without scheme, path, port, comma, whitespace, or control characters.
- CT-FE-009: Targeted broad `any` usage is now removed from `frontend/src/api`, `AdminDashboard.vue`, `AdminAnalyticsPanel.vue`, `OrderCenter.vue`, `HotelOrders.vue`, `UserProfile.vue`, `RouteDetail.vue`, `RoutePlanner.vue`, and `HotelBooking.vue`; route/hotel guardrail tests cover the target pages.
- CT-FE-011: RoutePlanner and HotelBooking user-facing labels, statuses, validation messages, cost rows, travel-kit/resume text, and locale-sensitive currency/date formatting now use i18n keys and locale-aware formatting helpers.
- CT-OPS-008: Supply-chain release inputs are fully pinned in the current worktree. GitHub Actions use full SHAs, Scrapler requirements are exact and hash-locked, Dockerfile/Compose/workflow image references include `@sha256:` digests, and `docker-digest-evidence.json` records the Docker Registry API evidence checked by `scripts/check-supply-chain-pins.mjs`.
- CT-SEC-PII-001: PII legacy/startup guard now uses the same production-intent predicate as `ProductionSafetyValidator`, so `APP_ENV=production`, cloud/Kubernetes signals, and production profiles all trigger the PII plaintext/legacy ciphertext scan.
- CT-OPS-027: Docker digest evidence validation now rejects bare record arrays and requires resolver metadata (`schemaVersion`, `generatedAt`, `verifier`, `lookupSource`, `targetPlatforms`) before release evidence can pass.
- CT-OPS-029: `CACHE_KEY_HMAC_SECRET` is independent from JWT material, required in production, rejected when blank/placeholder/short, and covered by backend validator, compose/upload/preflight scripts, docs, and ops guardrails.
- CT-SEC-PII-003: Recommendation similarity cache values and AI pending-start Redis metadata now store opaque HMAC labels instead of raw similar-user IDs, user IDs, or cache-key identifiers.
- CT-BE-039: Production AI quota and route-start controls now fail closed when Redis is missing or failing, preventing quota bypass or unlocked job starts during infrastructure outages.
- CT-FE-053: AdminDashboard spot, news, carousel, route, and hotel loaders now preserve existing rows on refresh failure, show retryable error states, and ignore stale responses.
- CT-FE-054: `QuestionDetail` now separates detail and answer load failures, keeps valid details visible when answers fail, reloads on route id changes, and guards stale detail/answer/like-status responses.
- CT-FE-055: Password-change success no longer triggers a stale profile-details reload that can turn a successful change into a false failure toast.

## Open / Residual Issues

No open residual issues are currently tracked after the 2026-06-09 17:18 product-hardening follow-up.

## Recently Closed Issues

### CT-OPS-031 - Closed - Production metrics and Redis protection defaults are fail-closed

- Status: Closed in the current worktree after ops/static tests and full validation passed.
- Implementation: `docker-compose.prod.yml` and `.env.example` now default `PUBLIC_METRICS_ENABLED=false`, and production compose explicitly defaults `RATE_LIMIT_REDIS_FAIL_CLOSED=true` plus `BRUTE_FORCE_REDIS_FAIL_CLOSED=true`. `scripts/deploy-preflight.ps1` rejects anonymous metrics and Redis protection fallback settings before release.
- Coverage: `deploy-preflight.test.mjs` covers negative release preflight runs for anonymous metrics and brute-force fallback; `production-security-defaults.test.mjs` asserts compose/env/prometheus defaults.
- Evidence: `npm run test:ops` passed with 30 tests. Full `npm run check` and `git diff --check` passed.

### CT-OPS-030 - Closed - Production Nginx rejects unmatched HTTPS hosts

- Status: Closed in the current worktree after ops validation passed.
- Implementation: `frontend/nginx.conf` now includes an HTTPS `default_server` that returns `444` for unmatched Host/SNI before the app server, and `frontend/Dockerfile` validates `NGINX_SERVER_NAME`, `NGINX_REDIRECT_HOST`, and `NGINX_CERT_DOMAIN` before rendering templates.
- Coverage: `nginx-security-config.test.mjs` asserts unmatched HTTP/HTTPS fail-closed behavior and runtime host validation. `deploy-preflight.test.mjs` validates production host values in actual PowerShell preflight runs.
- Evidence: `npm run test:ops` passed with 30 tests. Full `npm run check` and `git diff --check` passed.

### CT-BE-041 - Closed - Production rate limiting and brute-force protection fail closed on Redis outage

- Status: Closed in the current worktree after focused backend security coverage and full validation passed.
- Implementation: `RequestRateLimitFilter` now returns a generic 503 for sensitive routes when Redis is missing or unavailable under `app.security.rate-limit.redis-fail-closed=true`, while default read paths can still use local fallback. `LoginAttemptService` returns a short backend-unavailable throttle decision instead of resetting to per-instance counters when Redis is missing or unavailable under `app.security.brute-force.redis-fail-closed=true`. `ProductionSafetyValidator` rejects production deployments that disable Redis-backed rate limiting/brute-force protection or their fail-closed modes.
- Coverage: `RequestRateLimitFilterTest`, `LoginAttemptServiceTest`, `ProductionSafetyValidatorTest`, and `AdminSecurityPostureServiceTest` cover Redis failure/missing-backend behavior, production startup rejection, and admin security posture visibility.
- Evidence: `mvn -q -f backend/pom.xml "-Dtest=RequestRateLimitFilterTest,LoginAttemptServiceTest,ProductionSafetyValidatorTest,AdminSecurityPostureServiceTest" test` passed. Full `npm run check` passed.

### CT-BE-040 - Closed - Guide-chat usage accounting is opaque and production fail-closed

- Status: Closed in the current worktree after focused backend validation and full validation passed.
- Implementation: `GuideChatUsageService` now uses `CacheKeyHasher` HMAC labels for usage identity keys and rejects usage checks in production when Redis is absent or unavailable instead of falling back to local counters.
- Coverage: `GuideChatUsageServiceTest` covers production Redis outage denial and verifies Redis/memory keys do not contain raw user identifiers.
- Evidence: Full `npm run check` passed, including backend compile/test.

### CT-FE-057 - Closed - Admin hotel-order status updates show rollback/error state

- Status: Closed in the current worktree after focused frontend coverage and full validation passed.
- Implementation: `AdminDashboard.vue` labels the hotel-order status selector, disables it while a status update is pending, shows row-level failure text when the update fails, and rolls the visible status back to the original value.
- Coverage: `AdminDashboardHotelOrders.test.ts` covers the status update pending/error/rollback path along with existing admin stale/failure behavior.
- Evidence: `npm --prefix frontend run test -- --run src/views/AdminDashboardHotelOrders.test.ts` passed with 16 tests. Full `npm run check` passed with 49 frontend files / 304 tests.

### CT-FE-056 - Closed - RouteCommunity filter changes reset pagination and ignore stale responses

- Status: Closed in the current worktree after focused frontend coverage and full validation passed.
- Implementation: `RouteCommunity.vue` resets route filters and Q&A tag/sort changes back to page 0 after users browse later pages, and request IDs prevent older route/question responses from overwriting newer state.
- Coverage: `communityPagination.test.ts` covers filter/tag/sort page reset and stale route/Q&A response suppression.
- Evidence: `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts` passed with 24 tests. Full `npm run check` passed with 49 frontend files / 304 tests.

### CT-FE-055 - Closed - Password-change success cannot be overwritten by stale profile reloads

- Status: Closed in the current worktree after DOM coverage and full validation passed.
- Implementation: `UserProfile.vue` no longer calls `loadProfileDetails()` after a successful password change, so stale/expired session state cannot convert the success path into an error toast.
- Coverage: `UserProfileDomBehavior.test.ts` asserts forced password-change success clears the auth flag, removes the query, closes the modal, does not reload profile details, and does not show the failure toast. Existing comments stale-response tests now trigger refresh through comment actions instead of password changes.
- Evidence: focused frontend stale/UX coverage passed with 4 files / 42 tests. Full `npm run check` passed.

### CT-FE-054 - Closed - QuestionDetail handles route changes and partial load failures safely

- Status: Closed in the current worktree after focused DOM/a11y validation and full validation passed.
- Implementation: `QuestionDetail.vue` now separates question-detail and answers loading/failure state, preserves a loaded question when answers fail, keeps 404 as a true not-found state, watches `route.params.id`, and guards question, answer, and like-status requests with latest-request tokens.
- Coverage: `QuestionDetailDomBehavior.test.ts` covers retryable transient detail failures, answer-only failures with retry, 404 not-found behavior, and stale route-param response suppression. `questionDetailA11y.test.ts` remains green.
- Evidence: focused frontend stale/UX coverage passed with 4 files / 42 tests. Full `npm run check` passed.

### CT-FE-053 - Closed - AdminDashboard remaining admin lists preserve data on refresh failures

- Status: Closed in the current worktree after focused dashboard validation and full validation passed.
- Implementation: `AdminDashboard.vue` applies row-preserving loading/error/request-id handling to spot, news, carousel, route, and hotel loaders, matching the previously fixed user and hotel-order behavior.
- Coverage: `AdminDashboardHotelOrders.test.ts` now covers the expanded admin stale/failure behavior and passed with 15 tests.
- Evidence: focused frontend stale/UX coverage passed with 4 files / 42 tests. Full `npm run check` passed.

### CT-BE-039 - Closed - Production AI quota/start controls fail closed on Redis outage

- Status: Closed in the current worktree after focused backend validation and full backend validation passed.
- Implementation: `AiQuotaService` and `AiRouteGenerationJobService` now use the shared production-intent predicate and deny quota/start-lock operations in production when Redis is unavailable instead of falling back open.
- Coverage: `AiQuotaServiceTest` and `AiRouteGenerationJobServiceTest` cover production Redis outage denial/start failure paths.
- Evidence: focused backend AI/privacy/production-safety coverage passed. Full `mvn -q -f backend/pom.xml test` and full `npm run check` passed.

### CT-SEC-PII-003 - Closed - Redis values minimize user-identifying metadata

- Status: Closed in the current worktree after focused privacy validation and full validation passed.
- Implementation: Recommendation similarity cache values now store HMAC-labelled similar-user tokens, and AI pending-start metadata stores opaque `userKey` labels instead of raw user IDs or raw cache identifiers.
- Coverage: `RecommendationCacheServiceTest` and `AiRouteGenerationJobServiceTest` assert opaque Redis values while preserving cache/job behavior.
- Evidence: focused backend AI/privacy/production-safety coverage passed. Full `npm run check` passed.

### CT-OPS-029 - Closed - Cache-key HMAC secret is an independent production secret

- Status: Closed in the current worktree after backend, ops, and full validation passed.
- Implementation: `CacheKeyHasher` no longer falls back to JWT secret material outside local defaults. `ProductionSafetyValidator`, production compose, deployment upload/preflight scripts, env examples, and docs now require `CACHE_KEY_HMAC_SECRET` in production and reject blank, placeholder, or short values.
- Coverage: `CacheKeyHasherTest`, `ProductionSafetyValidatorTest`, `scripts/deploy-preflight.test.mjs`, and frontend ops guardrails cover the independent secret and 64-character minimum.
- Evidence: `opsConfigGuardrails.test.ts` passed with 9 tests, focused backend production-safety coverage passed, and full `npm run check` passed.

### CT-FE-051 - Closed - Route/community relation panels ignore stale responses

- Status: Closed in the current worktree after implementation, focused stale-response coverage, and full validation passed.
- Implementation: `RouteCommunity.vue` now guards route and question list loads with latest-request tokens, so older success/failure/finally paths cannot overwrite newer filter/page state or prematurely clear loading. `AdminHeritagePanel.vue` now guards relation loads by selected item plus latest request, and its top-level heritage item list preserves rows on failures, exposes retryable errors, and ignores stale refresh responses.
- Coverage: `communityPagination.test.ts` covers out-of-order route and question responses. `AdminHeritagePanel.relations.test.ts` covers stale relation success/failure, same-item reopen races, top-level heritage item refresh failures, and stale item-list responses.
- Evidence: `npm --prefix frontend run test -- --run src/views/AdminDashboardHotelOrders.test.ts src/components/AdminHeritagePanel.relations.test.ts src/views/communityPagination.test.ts src/views/UserProfileDomBehavior.test.ts src/api/cache.test.ts src/utils/sourceGuardrails.test.ts` passed with 6 files / 76 tests. Full `npm run check` passed.

### CT-FE-050 - Closed - AdminDashboard list failures no longer masquerade as empty data

- Status: Closed in the current worktree after DOM coverage and full validation passed.
- Implementation: `AdminDashboard.vue` now gives user and hotel-order lists independent loading/error state, visible retry actions, row-preserving transient-failure behavior, and latest-request guards for refresh races. Failed refreshes no longer clear existing rows into "0" or empty-table copy, and stale successes/failures cannot overwrite newer list state.
- Coverage: `AdminDashboardHotelOrders.test.ts` covers preserved hotel-order/user rows on refresh failure, retry recovery, stale hotel-order failures after newer success, and stale user responses after newer success.
- Evidence: `npm --prefix frontend run test -- --run src/views/AdminDashboardHotelOrders.test.ts` passed with 5 tests, and the focused 6-file / 76-test frontend suite plus full `npm run check` passed.

### CT-SEC-PII-002 - Closed - Redis/cache keys no longer expose raw user or IP identifiers

- Status: Closed in the current worktree after focused backend privacy coverage and full validation passed.
- Implementation: `CacheKeyHasher` provides keyed HMAC labels for cache keys. `IpLocationService` now uses HMAC labels for Spring cache keys, `RecommendationCacheService` uses HMAC user labels for Redis and local fallback cache keys, and `AiQuotaService` uses HMAC labels for daily quota keys plus route cache/start-lock keys instead of raw user IDs or route preference tuples.
- Coverage: `IpLocationServiceCacheKeyPrivacyTest`, `RecommendationCacheServiceTest`, and `AiQuotaServiceTest` assert cache/Redis/fallback keys do not contain literal IPs, user IDs, or route preferences while preserving hit/invalidate/cache behavior. `AiRouteGenerationJobServiceTest` remains green against the opaque cache-key contract.
- Evidence: `mvn -q -f backend/pom.xml '-Dtest=AiQuotaServiceTest,AiRouteGenerationJobServiceTest,IpLocationServiceCacheKeyPrivacyTest,RecommendationCacheServiceTest,CacheConfigSecurityTest' '-DfailIfNoSpecifiedTests=false' test` passed. Full `npm run check` passed.

### CT-FE-052 - Closed - Forced password-change profile flow has mounted DOM coverage

- Status: Closed in the current worktree after focused DOM validation passed.
- Implementation: Added mounted `UserProfileDomBehavior.test.ts` coverage for `mustChangePassword` / `?changePassword=1` opening the password modal before profile details load, successful password change clearing the auth store flag, removing the query with `router.replace('/profile')`, closing the modal, and then loading profile details.
- Evidence: `npm --prefix frontend run test -- --run src/views/UserProfileDomBehavior.test.ts` passed with 18 tests, and full `npm run check` passed.

### CT-OPS-028 - Closed - Deploy preflight coverage is included in the root ops gate

- Status: Closed in the current worktree after ops validation passed.
- Implementation: `package.json` now runs `scripts/deploy-preflight.test.mjs` as part of `npm run test:ops` with serial `--test-concurrency=1` execution, and `opsConfigGuardrails.test.ts` asserts the deploy preflight test cannot be silently removed from the ops gate.
- Evidence: `npm run test:ops` passed with 18 tests. The focused frontend security/ops suite passed with 42 tests, and full `npm run check` passed.

### CT-BE-038 - Closed - Order summary rows no longer expose contact PII

- Status: Closed in the current worktree after focused and full backend validation passed.
- Implementation: `OrderSummaryResponse` removed `customerName` and `customerPhone`, and `OrderCenterService` no longer maps masked contact fields into `/api/orders/my` summary rows. Detail responses still keep the existing masked contact fields for explicit detail views.
- Evidence: `mvn -q -f backend/pom.xml '-Dtest=OrderCenterServiceTest,OrderCenterControllerPageResponseContractTest' '-Dmaven.compiler.useIncrementalCompilation=false' test` passed. Full `npm run check` passed.

### CT-FE-049 - Closed - Favorites pagination ignores stale responses and keeps failure states explicit

- Status: Closed in the current worktree after DOM coverage and frontend validation passed.
- Implementation: `Favorites.vue` now tracks the latest favorites request before writing list/error/loading state, hides pagination while loading or showing errors, exposes `aria-busy`, assertive error alerts, localized retry/loading/pagination labels, and prevents stale page responses from overwriting the latest page.
- Coverage: `FavoritesDomBehavior.test.ts` covers localized labels, out-of-order pagination responses, and failed page loads that do not masquerade as an empty favorites list.
- Evidence: `npm --prefix frontend run test -- --run src/views/FavoritesDomBehavior.test.ts src/views/feedbackStates.test.ts` passed as part of the focused 6-file / 42-test suite. Full `npm run check` passed with 47 frontend test files / 275 tests plus production build.

### CT-FE-048 - Closed - Third-party scripts and external booking links now fail closed

- Status: Closed in the current worktree after focused security coverage and full frontend validation passed.
- Implementation: `amap.ts` verifies an existing loader script is `https://webapi.amap.com/maps` before reuse and resets the loader after failures. `recaptcha.ts` verifies existing scripts use trusted Google/reCAPTCHA origins, the fixed `/recaptcha/api.js` path, and the expected `render` parameter. `externalBooking.ts` now accepts direct booking URLs only when they are HTTPS, credential-free, and match built-in OTA host suffixes or explicit `VITE_EXTERNAL_BOOKING_ALLOWED_HOSTS`, otherwise it falls back to safe OTA search.
- Coverage: `amap.test.ts`, `recaptcha.test.ts`, `externalBooking.test.ts`, and `sourceGuardrails.test.ts` cover untrusted script/link rejection and allowlisted direct supplier URLs.
- Evidence: the focused frontend security/UX suite passed with 6 files / 42 tests. Full `npm run check` passed with frontend typecheck, 47 test files / 275 tests, and production build.

### CT-FE-047 - Closed - Profile route-list failures no longer masquerade as empty lists

- Status: Closed in the current worktree after implementation, DOM coverage, full frontend validation, and backend regression validation passed.
- Implementation: `UserProfile.vue` now gives the routes tab independent localized failure state, guarded refresh/append request tokens, accessible `role="alert"` retry UX, and append-specific retry intent. Latest non-401 refresh failures keep existing route rows/page metadata, stale route failures are ignored after newer wins, append failures keep visible rows while retrying the failed next page, and 401 route failures enter the existing auth-pending flow without showing retryable list errors or empty states.
- Coverage: `UserProfileDomBehavior.test.ts` covers latest route refresh failure plus retry recovery, stale failed route refresh ignored after newer route data wins, append failure preserving existing rows and retrying page 1, and 401 route failure avoiding retryable error/empty-state rendering.
- Evidence: `npm --prefix frontend run test -- --run src/views/UserProfileDomBehavior.test.ts` passed with 17 tests. `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/views/UserProfileDomBehavior.test.ts` passed with 37 tests. `npm --prefix frontend run typecheck` passed. `npm --prefix frontend run check` passed with 46 test files, 265 tests, and a production build. `mvn -q test` passed in `backend`. `git diff --check -- frontend/src/views/UserProfile.vue frontend/src/views/UserProfileDomBehavior.test.ts frontend/src/i18n/locales/zh.json frontend/src/i18n/locales/bo.json CODE_REVIEW_ISSUES.md` passed with only existing CRLF conversion warnings.

### CT-FE-046 - Closed - Profile booking/comment failures no longer masquerade as empty lists

- Status: Closed in the current worktree after implementation, DOM coverage, and focused frontend validation passed.
- Implementation: `UserProfile.vue` now gives scenic bookings, hotel bookings, and comments independent localized failure state, accessible `role="alert"` blocks, and retry buttons. Latest non-401 refresh failures no longer clear data or pagination into empty states; stale failed requests remain guarded by the existing request tokens; append failures use append-specific copy and retry the failed next-page intent instead of silently refreshing page 0. 401 list failures mark the auth flow as pending so ordinary empty/error states are not rendered while the API interceptor handles session expiry.
- Coverage: `UserProfileDomBehavior.test.ts` covers latest scenic/hotel/comments failures with retry, latest scenic append failure retrying page 1, stale scenic refresh failure, stale hotel/comments append failures, non-misleading empty states, and a 401 booking failure that does not render a retryable list error or empty state.
- Evidence: `npm --prefix frontend run test -- --run src/views/UserProfileDomBehavior.test.ts` passed with 13 tests. `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/views/UserProfileDomBehavior.test.ts` passed with 33 tests. `npm --prefix frontend run typecheck` passed. `npm --prefix frontend run check` passed with 46 test files, 261 tests, and a production build. `mvn -q test` passed in `backend`. `git diff --check -- frontend/src/views/UserProfile.vue frontend/src/views/UserProfileDomBehavior.test.ts frontend/src/i18n/locales/zh.json frontend/src/i18n/locales/bo.json CODE_REVIEW_ISSUES.md` passed with only existing CRLF conversion warnings.

### CT-FE-045 - Closed - Profile/account stale-response regression is guarded

- Status: Closed in the current worktree after implementation, focused DOM regression tests, and the frontend release gate passed.
- Implementation: `UserProfile.vue` now uses paginated request guards for scenic bookings, hotel bookings, and comments. Full refreshes clear stale append state, expose refresh busy state, and block load-more while an authoritative refresh is in flight. Cancel handlers await their post-cancel refresh, delete handlers do an immediate local removal followed by authoritative refresh, and delete metadata is clamped so users cannot see impossible page text such as `3 / 2`.
- Coverage: `UserProfileDomBehavior.test.ts` now controls deferred stale/fresh responses for scenic booking refreshes, cancel-triggered refreshes versus load-more clicks, delete backfill/clamp behavior, stale hotel append superseded by refresh, and stale comments refresh/append paths. `communityPagination.test.ts` was updated so the existing scenic-booking delete pagination baseline reflects the backend-authoritative refresh contract.
- Evidence: `npm --prefix frontend run test -- --run src/views/UserProfileDomBehavior.test.ts` passed with 5 tests. `npm --prefix frontend run test -- --run src/views/communityPagination.test.ts src/views/UserProfileDomBehavior.test.ts` passed with 25 tests. `npm --prefix frontend run typecheck` passed. `npm --prefix frontend run check` passed with 46 test files, 253 tests, and a production build. `git diff --check -- frontend/src/views/UserProfile.vue frontend/src/views/UserProfileDomBehavior.test.ts frontend/src/views/communityPagination.test.ts CODE_REVIEW_ISSUES.md` passed with only existing CRLF conversion warnings.

### CT-FE-044 - Closed - Frontend UX/a11y tests now include runtime DOM behavior

- Status: Closed in the current worktree after a green frontend check.
- Evidence: Added mounted jsdom tests in `ScenicSpotsDomBehavior.test.ts`, `HotelOrdersDomBehavior.test.ts`, and `OrderCenterDomBehavior.test.ts`, with shared test utilities in `domBehaviorTestHelpers.ts`.
- Coverage: ScenicSpots now verifies loading/result live regions, API-rendered cards, search filtering, Escape clearing, and category `aria-pressed` behavior. HotelOrders verifies booking confirmation dismissal, accessible error/empty states, and phone masking. OrderCenter verifies initial loading announcement, selected filter state, keyboard card selection, detail-panel linkage, filter reset, and load-more busy/disabled state.
- Follow-up watch item: Review identified stale-response hardening for some account/order list refresh paths as a separate UX robustness opportunity; it is no longer part of the source-string-only test-depth residual.
