# Code Review Issues

Last updated: 2026-06-08
Last reviewed: 2026-06-08 21:04:09 +08:00

This file tracks issues re-checked against the current worktree. Items marked fixed have code and tests in this worktree; residual items remain candidates for the next 10-minute review cycle.

## Verification Snapshot

- Final multi-agent production-readiness gate on 2026-06-08 21:04 +08:00: the four development agents, one parallel test agent, and one parallel review agent all returned and were closed. Follow-up review findings were addressed locally: SSE clean EOF now leaves route generation interrupted/resumable instead of falsely complete, favorite duplicate-key races are idempotent, and public question lists use summary DTOs without full `content`. Full backend `mvn -q -f backend/pom.xml test` passed, full frontend `npm --prefix frontend run check` passed (typecheck, 40 Vitest files / 185 tests, and production build), local and production compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, workflow YAML parsing passed, PowerShell parser checks passed for deployment scripts, `cmd /c stop.bat --dry-run` passed, and the deployment archive dry-run passed with 798 Git-tree files approved. This moved CT-BE-024, CT-BE-025, CT-BE-026, CT-BE-027, CT-FE-019, CT-FE-020, CT-FE-021, CT-FE-022, CT-OPS-016, and CT-OPS-017 to fixed status.
- Latest long-review cycle on 2026-06-08 20:53 +08:00: all four review agents and both testing agents returned. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile` and the backend testing agent's 34-class / 224-test focused suite; local conflict resolution also passed `mvn -q -f backend/pom.xml -Dtest="SharedRouteControllerPaginationTest,TravelQAControllerPaginationTest,SharedRouteServiceTest,TravelQAServiceTest,ProductionSafetyValidatorTest,RecaptchaServiceTest" test`. Frontend/OPS validation passed a 14-file / 70-test focused Vitest suite, `npm --prefix frontend run typecheck`, `git diff --check`, and both compose config checks with `.env.example`. Review plus local evidence moved CT-BE-027, CT-FE-020, CT-FE-022, and CT-OPS-016 to fixed status, narrowed CT-BE-026 to the remaining favorite race, and added CT-BE-028, CT-FE-023, CT-FE-024, and CT-OPS-018 as current residuals.
- Final multi-agent production-readiness gate on 2026-06-08 20:37 +08:00: the four development agents, one parallel test agent, and one parallel review agent all returned and were closed. Full backend `mvn -q -f backend/pom.xml test` passed, full frontend `npm --prefix frontend run check` passed (typecheck, 38 Vitest files / 175 tests, and production build), local and production compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, the deployment archive dry-run passed with 793 Git-tree files approved, and workflow YAML parsing passed. Follow-up fixes moved CT-BE-021, CT-BE-022, CT-BE-023, CT-FE-015, CT-FE-016, and CT-OPS-015 to fixed status; the PII legacy guard now rejects placeholder legacy keys and verifies sample legacy ciphertext decryptability instead of trusting any nonblank key.
- Latest long-review cycle on 2026-06-08 20:31 +08:00: all four review agents and both testing agents returned. Backend validation passed `mvn -q -f backend/pom.xml -DskipTests compile` and a focused 11-class / 87-test Maven suite covering order center, payment audit, PII crypto/backfill, recommendation limits, itinerary pagination, and companion inference. Frontend validation passed the 7-file / 30-test focused Vitest suite and `npm --prefix frontend run typecheck`. OPS validation reported `git diff --check`, both `.env.example` compose config checks, and PowerShell parser checks passing, with only LF-to-CRLF and stale browser data warnings. Review plus local evidence removed stale CT-BE-021/022/023 and CT-FE-015/016 open entries, kept CT-OPS-006 fixed, and added CT-BE-024 through CT-BE-027, CT-FE-019 through CT-FE-022, and CT-OPS-016/017 as current residuals.
- Final production-readiness gate on 2026-06-08 20:11 +08:00: full `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (typecheck, 37 Vitest files / 170 tests, and production build), both compose config checks with `.env.example` passed, `git diff --check` passed with LF-to-CRLF warnings only, and `scripts/New-DeploymentArchive.ps1` produced a deployment archive dry-run with 787 Git-tree files approved and no forbidden local uploads/generated artifacts.
- Follow-up recheck on 2026-06-08 20:05 +08:00: PowerShell parser checks passed for `upload-server.ps1` and `deploy-new-server-http.ps1`; the embedded remote bash scripts from both files passed `bash -n`; a temp-file bash harness passed for allowed project paths, rejected `..`/`.`/`//`/outside-prefix inputs, rejected project symlink escapes, and rejected `.env`/`data`/`logs`/`certs` symlinks. `docker compose --env-file .env.example -f docker-compose.yml config --quiet`, `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet`, and `git diff --check` passed apart from LF-to-CRLF warnings. The current `upload-server.ps1` and `deploy-new-server-http.ps1` reject non-normalized project paths, require `realpath`, re-check the created path with `realpath -e`, refuse preserved symlink paths, and use `cd -P` plus `find .` before destructive replacement, so CT-OPS-013 is fixed in the current worktree and no longer belongs in the open list.
- PII safety follow-up on 2026-06-08 20:09 +08:00: `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.common.security.PiiCryptoConverterTest,com.tibet.tourism.common.security.PiiBackfillRunnerTest,com.tibet.tourism.common.security.PiiLegacyCiphertextStartupGuardTest" test` passed, and `mvn -q -f backend/pom.xml -DskipTests compile` passed. `PiiCryptoConverter` now fails closed on encrypted-prefix decrypt failures instead of returning raw `enc:*` ciphertext, and the prod-only `PiiLegacyCiphertextStartupGuard` fails startup when legacy `enc:v1:%` rows remain without `PII_ENCRYPTION_KEY`.
- Latest long-review cycle on 2026-06-08 19:55 +08:00: all four review agents and both testing agents returned. Frontend testing passed `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. The backend testing agent initially hit a transient compile failure while `reserveRefundTransaction(...)` call sites were changing, but the current worktree now passes `mvn -q -f backend/pom.xml -DskipTests compile` and the focused backend suite covering order center, companion inference, recommendation limits, and PII backfill/crypto. Review agents revalidated all remaining BE/FE/OPS residuals, found CT-OPS-014 fixed in the current scripts/compose wiring, and added CT-BE-021, CT-BE-022, CT-BE-023, and CT-OPS-015 as new residuals.
- Final production-readiness sweep on 2026-06-08 19:47 +08:00: `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.OrderCenterControllerPrivacyTest" test` passed, full `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (37 Vitest files / 170 tests plus typecheck/build), `git diff --check` passed with LF-to-CRLF warnings only, both compose config checks with `.env.example` passed, and `scripts/New-DeploymentArchive.ps1` successfully produced a Git-tree deployment archive after new production/deployment files were added to the Git index. Final review follow-up fixed stale same-order/same-refund transaction reservation recovery, reservation order-delete cascade, and deployment archive expected-vs-actual content checks.
- Latest heartbeat cycle on 2026-06-08 19:37 +08:00: all four review agents and both testing agents returned. Frontend validation passed in the testing agent with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. Backend validation passed in the testing agent with 10 focused test classes / 71 tests plus `mvn -q -DskipTests compile`. Local follow-up passed `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.spot.application.CompanionInferenceServiceTest" test`, showing payment/refund transaction-number reservations are covered and companion-inference logs now render `36.00%` instead of literal `{:.2f}`. `git diff --check` passed with LF-to-CRLF warnings only after removing trailing whitespace in the companion-inference log line. Review agents revalidated the remaining backend, frontend, and OPS residuals; CT-BE-018, CT-BE-019, and CT-BE-020 now have current code/test evidence for fixed status.
- Latest continuation cycle on 2026-06-08 19:18 +08:00: all four review agents and both testing agents returned. Frontend validation passed with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` (7 files / 30 tests) and `npm run typecheck`. Backend validation passed two focused Maven suites covering orders, route/itinerary, PII, recommendation, companion inference, AI route, guide chat, price updates, current-user, controller privacy, and log guardrails (76 tests + 86 tests), plus `mvn -q -DskipTests compile`. Local follow-up also passed the recommendation/companion focused suite and confirmed production recommendation paths no longer call unbounded `historyRepository.findByUserId(...)`. Review agents confirmed CT-BE-014, CT-BE-016, and CT-BE-017 are fixed in the current worktree; revalidated frontend and OPS residuals; narrowed CT-BE-018 to cross-order payment transaction-number races; and added refund transaction-number and companion-inference log-format residuals.
- Latest heartbeat cycle: all four review agents and both testing agents returned by 2026-06-08 19:04 +08:00. Frontend focused tests passed locally with `npm run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts` (5 files / 18 tests) after the frontend test agent also reported that suite and `npm run typecheck` passing. Backend testing initially reported a `RecommendationServiceTest` testCompile failure from corrupted string literals, but the current worktree has repaired those lines and a local rerun of `mvn -q -Dtest="com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.dto.CreateOrderRequestValidationTest,com.tibet.tourism.modules.route.application.ItineraryServiceTest,com.tibet.tourism.modules.route.web.ItineraryControllerPaginationTest,com.tibet.tourism.common.security.PiiCryptoConverterTest,com.tibet.tourism.common.security.PiiBackfillRunnerTest,com.tibet.tourism.modules.recommendation.application.RecommendationServiceTest" test` passed. `git diff --check` passed with only LF-to-CRLF warnings. The reported repository-noise heredoc CI risk did not reproduce: parsing `.github/workflows/ci.yml` with the frontend `yaml` package showed the `EOF` at shell column 1, and `bash -n` on the parsed `run` block exited 0.
- Latest production-readiness gates on 2026-06-08 19:15 +08:00: `mvn -q -f backend/pom.xml test` passed, `npm --prefix frontend run check` passed (37 Vitest files / 170 tests plus typecheck/build), `git diff --check` passed with LF-to-CRLF warnings only, and both `docker compose --env-file .env.example -f docker-compose.yml config --quiet` plus `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` passed. Bare compose config without env still fails on required secret variables, which is expected for production safety.
- Backend: `mvn -q -DskipTests compile`, focused security/auth/user tests, and latest full `mvn -q -f backend/pom.xml test` passed again on 2026-06-08 18:45 +08:00.
- Frontend: latest full `npm --prefix frontend run check` passed on 2026-06-08 18:45 +08:00 (`vue-tsc`, 37 Vitest files / 170 tests, and production build).
- Latest focused follow-up: `npm run test -- --run src/utils/devProxyTarget.test.ts` passed on 2026-06-08 16:55 +08:00 (1 file / 3 tests). `cmd /c stop.bat --dry-run` passed on 2026-06-08 16:57 +08:00 and resolved the project to `/mnt/c/Users/Suli/Desktop/colorful-tibet`. `npm run test -- --run src/utils/scriptSecurity.test.ts src/utils/amap.test.ts src/utils/recaptcha.test.ts` passed on 2026-06-08 17:00 +08:00 (3 files / 8 tests). `npm run test -- --run src/api/stream.test.ts` passed on 2026-06-08 18:01 +08:00 (1 file / 9 tests). `npm run test -- --run src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts` passed on 2026-06-08 18:05 +08:00 (2 files / 8 tests). `mvn -q -Dtest="com.tibet.tourism.common.security.*Test" test` passed on 2026-06-08 18:09 +08:00. `npm run test -- --run src/views/routeHotelA11y.test.ts` passed on 2026-06-08 18:11 +08:00 (1 file / 7 tests) and again on 2026-06-08 18:25 +08:00. `npm run test -- --run src/components/mobileNavigationA11y.test.ts` passed again on 2026-06-08 18:14 +08:00 (1 file / 5 tests).
- Compose preflight check: `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` and `docker compose --env-file .env.example -f docker-compose.yml config --quiet` exited 0 on 2026-06-08 17:35 +08:00 after the Redis/MySQL healthcheck secret handling update. A focused scan for `redis-server ... --requirepass`, `redis-cli -a`, and `mysqladmin ... -p` returned no matches in either Compose file.
- Negative compose preflight for CT-OPS-009: with all other `.env.example` values loaded and `SUPER_ADMIN_USERNAME` omitted, `docker compose -f docker-compose.prod.yml config --quiet` now exits 1 on 2026-06-08 18:02 +08:00 with the required-variable error. Positive production compose config still exits 0 with `.env.example`.
- Earlier testing agents completed: backend `mvn -q test` passed with 95 Surefire XML files / 796 tests; frontend `npm run check` passed with 35 Vitest files / 164 tests and production build.
- Earlier 4 code-review agents returned by 2026-06-08 18:48 +08:00. They confirmed CT-BE-011/012/013, CT-FE-012/013, and CT-OPS-004/010/011/012 no longer match their original open evidence; revalidated CT-OPS-006 as only partially mitigated; and added CT-BE-016/017, CT-FE-015/016/017/018, and CT-OPS-013/014.
- Diff hygiene: `git diff --check` passed on 2026-06-08 18:53 +08:00; it only reported LF-to-CRLF warnings.
- OPS follow-up: `.github/workflows/ci.yml` parsed with `yaml@2.6.1`, `python -m yamllint -d "{extends: relaxed, rules: {line-length: disable, new-lines: disable, truthy: disable}}" .github/workflows/ci.yml` passed, and actionlint v1.7.8 passed on 2026-06-08 18:43 +08:00. PowerShell parser checks passed for `upload-server.ps1`, `deploy-new-server-http.ps1`, and `scripts/New-DeploymentArchive.ps1`. A deployment archive dry-run passed with 771 Git-tree files approved and no forbidden local uploads/generated artifacts. `docker compose --env-file .env.example -f docker-compose.yml config --quiet` and `docker compose --env-file .env.example -f docker-compose.prod.yml config --quiet` passed again.
- Latest focused follow-up for this review: `npm --prefix frontend run test -- --run src/api/endpointRegistry.test.ts src/stores/auth.test.ts src/views/orderProfileA11y.test.ts src/views/richTextXss.test.ts src/components/mobileNavigationA11y.test.ts src/components/appShellA11y.test.ts src/views/routeHotelA11y.test.ts` passed on 2026-06-08 18:44 +08:00 (7 files / 30 tests). `mvn -q -f backend/pom.xml -Dtest="com.tibet.tourism.common.security.*Test,com.tibet.tourism.modules.order.application.OrderCenterServiceTest,com.tibet.tourism.modules.order.web.BookingControllerAnonymousOwnerTest,com.tibet.tourism.modules.order.web.dto.CreateOrderRequestValidationTest" test` passed on 2026-06-08 18:44 +08:00.
- Known non-failing warnings: stale Browserslist/baseline data, Rolldown pure annotation warnings in `@vueuse/core`, and expected negative-path backend logs.

## Fixed This Cycle

- CT-SEC-001: Authenticated `/api/guide/chat` now requires signed double-submit XSRF when an `AUTH_TOKEN` cookie is present. Anonymous public POST behavior remains allowed with browser metadata checks.
- CT-SEC-002: Controller authorization matrix now discovers real module `@RestController` classes automatically and covers previously omitted controllers.
- CT-SEC-003: CSRF trusted origins no longer accept wildcard patterns that CORS rejects when credentials are enabled.
- Private uploads: `/uploads/private/**` is excluded from public reads and denied for authenticated users too.
- CT-FE-001: Admin 401 responses now use centralized auth expiration unless a request explicitly sets `skipAuthRedirect`.
- CT-FE-002: Reported hardcoded endpoint paths were moved to `frontend/src/api/endpoints.ts` / auth modules, including follow-up profile/community/route share paths.
- CT-FE-003: Public rich text now strips remote HTTP(S) images while preserving local, same-origin, and bounded safe data images.
- CT-FE-004: SSE parsing now enforces buffer/frame byte limits and cancels hostile streams on overflow.
- CT-BE-002: Redis guide-chat window-limit rejections no longer consume daily quota; race compensation was added.
- CT-BE-003: Duplicate AI route job creation now has single-JVM serialization plus Redis start locks with TTL for cross-instance duplicate starts.
- CT-BE-004: AI route job query/stream now falls back to persisted RUNNING records when memory state is missing, and startup recovery marks stale RUNNING records failed instead of leaving them forever active.
- CT-BE-005: Itinerary expansion now uses Hibernate batch fetching on days/items and related spot/hotel/room type references to reduce N+1 pressure.
- CT-BE-001: Scrapling prices are no longer hardcoded reference-only when upstream explicitly marks a result as publishable/non-reference; default behavior remains conservative.
- CT-BE-007: Batch price updates now delegate single-spot work to `SingleSpotPriceUpdateService` with `REQUIRES_NEW`, including async batch paths.
- CT-BE-008: Price update focused tests were updated for constructor injection and now compile/pass.
- CT-FE-005: Live2D rendering is gated behind desktop interaction or idle loading, with a lightweight placeholder before the model loads.
- CT-FE-006: Admin dashboard/community/heritage clickable panels now expose keyboard semantics with `role`, `tabindex`, accessible labels, and Enter/Space handlers; action controls use real buttons.
- CT-FE-007: Heat map remote geo JSON fallback is now disabled by default and gated behind `VITE_HEATMAP_REMOTE_GEO_FALLBACK_ENABLED`; local geo JSON remains the default path.
- CT-FE-008: AMap and reCAPTCHA dynamic loaders now apply shared third-party script security attributes, including CSP nonce, optional SRI, and `crossOrigin`, with env/meta/runtime nonce support covered by focused tests.
- CT-FE-010: AI route stream/job APIs now use `endpoints.routes.generateStream`, `generateJob`, `generateJobDetail(jobId)`, and `generateJobStream(jobId)` from the endpoint registry; `frontend/src/api/stream.test.ts` covers the generated fetch URLs.
- CT-OPS-001: Vite dev proxy target now derives from remote `VITE_API_BASE_URL`/`VITE_DEV_PROXY_TARGET` through `resolveDevProxyTarget`, so `frontend/start-remote.bat` no longer silently falls back to local `localhost:8080` for `/api` during remote debugging.
- CT-OPS-002: Production Compose now defaults `PUBLIC_METRICS_ENABLED=true` for the bundled internal Prometheus scrape, with docs/comments requiring trusted backend/monitoring networks or an explicit protected metrics path when disabled.
- CT-OPS-003: Production Compose no longer exposes Redis/MySQL credentials through the old long-lived `redis-server --requirepass`, `redis-cli -a`, or `mysqladmin -p...` command arguments; Redis now boots from a restricted temporary config file with an escaped `requirepass`, Redis healthcheck uses `REDISCLI_AUTH`, and local/production MySQL healthchecks use a temporary `--defaults-extra-file`.
- CT-OPS-005: `stop.bat` now uses Windows `>nul`, derives the project path from `%~dp0`, detects/overrides the WSL distribution, and supports `--dry-run`.
- CT-OPS-009: Production Compose now requires `SUPER_ADMIN_USERNAME` during compose interpolation, so missing super-admin configuration fails during preflight instead of later at backend startup.
- CT-OPS-010: Deployment archives are now produced through `scripts/New-DeploymentArchive.ps1`, which archives a temporary Git tree instead of enumerating the working directory, fail-fast rejects untracked production/deployment files that would be omitted, excludes untracked/ignored artifacts by construction, rejects forbidden deployment paths such as `output`, `data`, `logs`, `certs`, `backend/uploads`, `backend/target`, `frontend/dist`, and virtualenv/cache paths, and performs pre-upload tar contents checks including entry count plus exact ASCII-path expected-vs-actual comparison before either deployment script uploads.
- CT-OPS-011: The HTTP demo deployment no longer executes `https://get.docker.com` by default. Missing Docker now fails with preinstall guidance; convenience-script bootstrap requires explicit `-BootstrapDocker`, a 64-character `-DockerInstallScriptSha256`, and a pinned `-DockerInstallVersion`, then verifies SHA256 before running with `VERSION=...`.
- CT-OPS-012: CI now includes a workflow lint job for actionlint/yamllint, and the frontend Docker image build/scan passes deterministic production-like Vite build args for API base, reCAPTCHA, and AMap.
- CT-OPS-014: HTTP demo deploy now requires explicit `-DemoOnly`, writes `FRONTEND_HOST_BIND=0.0.0.0` alongside `FRONTEND_HOST_PORT=80`, uses the compose bind override instead of the loopback default, and fails when a public `SiteUrl` check fails.
- CT-OPS-013: Remote deployment project paths now reject `..`, `.`, repeated slash, and other non-normalized inputs; both deployment scripts require `realpath`, normalize `$PROJECT_DIR` under `/opt/colorful-tibet`, re-check the created directory with `realpath -e`, and use `cd -P` before destructive replacement so symlink/physical-path escapes are refused.
- CT-OPS-004: Deployment scripts no longer make `data` and `logs` world-writable. They derive `BACKEND_APP_UID/GID` from `.env` with safe defaults, `chown` those paths to the backend container user, and set directories/files to `750`/`640`.
- CT-OPS-006: PII decryption failures now emit low-cardinality `pii_decrypt_failure_total` metrics and structured security logs without raw ciphertext/PII. `PiiCryptoConverter` fails closed on encrypted-prefix decrypt failures instead of returning raw `enc:*` ciphertext to entity/response mapping, rejects placeholder legacy keys, and the prod-only `PiiLegacyCiphertextStartupGuard` scans legacy `enc:v1:%` rows even when a legacy key is configured so wrong keys fail startup through sample decryptability checks. `PiiBackfillRunner` skips undecryptable encrypted values instead of wrapping them into new v2 ciphertext, deployment validation rejects placeholder `PII_ENCRYPTION_KEY`, and Prometheus has a PII decrypt failure alert.
- CT-BE-009: `/api/orders/my` now uses sanitized `Pageable` with default size 20 and max size 50 while keeping the response body as a compatibility array plus pagination headers.
- CT-BE-010: Refund review/completion now has a production admin path. `OrderCenterController.java` exposes `/api/admin/orders/{orderId}/refunds/{refundId}/review`; `OrderCenterService` supports approve/reject/complete transitions, provider refund transaction-number conflict checks, rejected-cancellation state restoration, and voucher/inventory release on completed refunds; `OrderCenterServiceTest` covers approval, completion, rejection, duplicate provider refund transaction numbers, and partial cancellation refunds closing entitlements.
- CT-BE-014: Recommendation history reads now use bounded recent queries across the recommendation path and companion inference. `UserVisitHistoryRepository` centralizes recent/top-N queries, recommendation strategies use the bounded methods, and `CompanionInferenceService` uses recent visit history plus a paged recent booking sample.
- CT-BE-016: Late successful payment callbacks against closed or no-longer-payable orders no longer create successful `PaymentTransaction` rows. The service records a rejected callback audit and returns the current order state without reviving or polluting the order ledger.
- CT-BE-017: Order creation now catches idempotency-key unique constraint races, re-reads the existing order by user/idempotency key, and avoids creating duplicate inventory locks.
- CT-BE-018: Cross-order payment callback transaction-number races now claim the provider transaction number through `PaymentTransactionReservationService` before attaching a new payment transaction. `V20__reserve_payment_transaction_numbers.sql` adds a primary-key-backed reservation table with historical transaction backfill, conflicts re-read existing transactions/reservations, same-order stale reservations can recover when payload matches, and `OrderCenterServiceTest` covers both another-order races and same-order retry recovery.
- CT-BE-019: Refund provider transaction numbers now use the same atomic reservation path before refund state mutation. Explicit and generated refund transaction numbers reserve before completion, conflict checks include reservations, same-refund stale reservations can recover by `refundNo`, the reservation FK cascades when orders are deleted, and `OrderCenterServiceTest` covers both concurrent provider refund transaction-number claims and same-refund retry recovery.
- CT-BE-020: Companion inference success logs now pre-format confidence percentages with `Locale.ROOT` and use normal SLF4J `{}` placeholders, removing the literal `{:.2f}` output in logs.
- CT-BE-011: Profile stats now use count queries instead of loading complete route/booking histories, and `/api/auth/me/comments` now pages spot and route comments with page metadata.
- CT-BE-012: `CreateOrderRequest.items` now has `@Size(max = 20)` and service-level validation rejects empty or oversized item lists before repository lookups.
- CT-BE-013: Legacy `/api/bookings/my` now uses sanitized paging with default size 20 and max size 50 while preserving the array body and returning pagination headers.
- CT-BE-015: Security WebMvc slice tests no longer fail on missing access-denied-handler wiring; the focused `com.tibet.tourism.common.security.*Test` suite passes in the current worktree.
- CT-FE-012: `auth/me` and scenic spot comment like/liked calls now use endpoint registry helpers, with endpoint/auth tests covering the generated URLs.
- CT-FE-013: OrderCenter order cards now have keyboard semantics, and saved routes in UserProfile use real `router-link` navigation instead of a mouse-only heading.
- CT-FE-014: Footer About Us no longer uses a fake `href="#"`; it is now a keyboard-focusable button that opens the contact modal, and `frontend/src/components/mobileNavigationA11y.test.ts` asserts the footer has no fake share/about links.
- CT-BE-021: Public shared-route comments and travel-question answers are now paged with bounded/sanitized `Pageable` and pagination response metadata/headers.
- CT-BE-022: Saved AI route history now uses paged summary DTOs for `/api/routes/ai/saved`, with full generated content fetched only from the detail endpoint.
- CT-BE-023: `/api/routes/my-routes` now uses bounded paging and `SharedRouteSummaryResponse`, so current-user route lists no longer return every row or full route content.
- CT-FE-015: The broad substring-based 401 redirect exemptions were removed; only explicit `skipAuthRedirect` bypasses auth expiration, and protected booking-cancel/route-share 401 cases are covered by `frontend/src/api/client.test.ts`.
- CT-FE-016: `ContactModal` now has dialog semantics, labelled controls, focus management, and Escape/Tab keyboard handling.
- CT-OPS-015: CI and `scripts/New-DeploymentArchive.ps1` now reject suspicious tracked root shell artifacts such as `%s`, quote-prefixed fragments, semicolon leftovers, and control-token debris before they can be treated as approved deployable Git-tree entries.
- CT-BE-027: Route/question like handlers now return `LikeResult` values from the like/unlike service paths and read like counts through no-view-increment count lookups, so like/unlike no longer calls the detail loaders that increment `viewCount`. Covered by the current focused community tests.
- CT-BE-024: Public shared-route list responses now use `SharedRouteSummaryResponse`, so anonymous list browsing no longer transfers full route `content`; detail responses still return full content.
- CT-BE-025: Public heritage inheritor and event subresources now return capped `PageResponse` payloads with stable sorting instead of unbounded lists.
- CT-BE-026: Favorite creation now uses flush-time duplicate handling and returns the current favorite state/count idempotently under concurrent duplicate requests.
- CT-FE-020: Route comments, question answers, and profile shared routes now use `readPaginatedResponse`, preserve page metadata, and render load-more controls instead of silently showing only the first backend page.
- CT-FE-022: The route guard now preserves protected/admin `to.fullPath` through `createLoginRedirect`, and login consumes a same-origin sanitized redirect before falling back by role; `frontend/src/router/authRedirect.test.ts` covers external, looping, and control-character redirects.
- CT-OPS-016: Production reCAPTCHA defaults are no longer overridden off by `.env.example`; the example sets backend, frontend, and registration-required flags to true, and `ProductionSafetyValidator` rejects production deployments with reCAPTCHA disabled or placeholder site/secret keys.
- CT-FE-019: AI route job streaming now reports clean EOF without a terminal event as interrupted, preserves partial content/job id, clears loading state, and enables resume instead of leaving the route planner stuck.
- CT-FE-021: Scenic spot liked-state checks now run through bounded parallel/all-settled loading instead of serial per-comment awaits, reducing comment-page latency and isolating partial failures.
- CT-OPS-017: Local WSL helper commands now quote project paths through a bash-safe single-quote encoder in both `start.ps1` and `stop.bat`.
- Frontend rich-text XSS regression: `frontend/src/views/richTextXss.test.ts` now covers hostile Markdown/HTML payloads such as `javascript:`, event handlers, SVG, iframe, and unsafe data images through real DOM insertion assertions.

## Open / Residual Issues

### CT-BE-004 - Medium - AI route job recovery is query-safe but not fully live-stream recoverable

- Status: Partially fixed.
- Evidence: `backend/src/main/java/com/tibet/tourism/modules/ai/application/AiRouteGenerationJobService.java:288` through `292` can return a record-backed RUNNING snapshot when in-memory `jobs` state is missing, but that object is not connected to the original producer/subscriber list. The durable fallback lookup at `AiRouteGenerationJobService.java:451` through `456` only works by `jobId`, while `backend/src/main/java/com/tibet/tourism/modules/ai/application/AiRouteRecordService.java:78` and `109` clear `jobId` on completed/failed records.
- Residual risk: Query/stream no longer returns `not found` for persisted RUNNING records, and startup recovery marks stale RUNNING records failed. True cross-instance live SSE recovery still needs shared stream state, Redis/pub-sub, a message table, or worker-affinity routing while a job is actively running. Completed/failed jobs also stop being durable by job URL after in-memory retention cleanup because terminal records no longer retain the external `jobId`.
- Suggested fix: Keep a durable external job/request id until job URLs expire, or introduce a separate job table/event log that can serve terminal snapshots and replayable stream history after memory cleanup.

### CT-BE-005 - Medium - Itinerary list/detail still expands full object graphs

- Status: Partially fixed.
- Evidence: `backend/src/main/java/com/tibet/tourism/modules/route/application/ItineraryService.java:103` through `111` now clamps `Pageable` and calls `itineraryRepository.findByUserId(user.getId(), safePageable)`, with repository paging at `backend/src/main/java/com/tibet/tourism/modules/route/infra/ItineraryRepository.java:12`. The residual list path still maps every row through full `toResponse()` at `ItineraryService.java:484`, expanding days/items and related spot/hotel/room type references at `ItineraryService.java:500` through `521`.
- Residual risk: Top-level itinerary counts are bounded, but list responses can still perform avoidable lazy batches and transfer full detail DTOs where summaries or detail-specific fetch plans would be safer.

### CT-BE-001 - Medium - Reference-price review storage is still missing

- Status: Partially fixed.
- Evidence: `backend/src/main/java/com/tibet/tourism/modules/spot/application/SingleSpotPriceUpdateService.java:49` through `52` skips reference-only or low-confidence fetched prices without saving a durable observation. `backend/src/main/java/com/tibet/tourism/modules/spot/domain/ScenicSpot.java:42` exposes formal ticket-price fields, but there is no review/reference/evidence table or field for rejected candidates.
- Residual risk: High-confidence explicit Scrapling publish candidates can persist, but non-publishable/reference results still have no durable review/reference queue.

### CT-BE-006 - Medium - Recommendation behavior writes and cache invalidation may be missing

- Status: Needs product confirmation.
- Evidence: `RecommendationService` reads `UserVisitHistory` and exposes `invalidateUserCache()`, but runtime `UserVisitHistory` writes and business calls to `invalidateUserCache()` were not found outside seed/test flows. Representative gaps: `backend/src/main/java/com/tibet/tourism/modules/community/web/FavoriteController.java:79` saves favorites without recommendation invalidation, and `backend/src/main/java/com/tibet/tourism/modules/spot/web/ScenicSpotController.java:79` returns spot detail without visit-history write.
- Suggested fix: If recommendations should react to views, favorites, ratings, or orders, add behavior writes and user cache invalidation at those actions.

### CT-BE-009 - Medium - Order list is paged but still expands every child collection

- Status: Partially fixed.
- Evidence: `backend/src/main/java/com/tibet/tourism/modules/order/application/OrderCenterService.java:260` through `271` now reads a sanitized `Pageable` via `orderRepository.findByUserId(user.getId(), safePageable)`, and `backend/src/test/java/com/tibet/tourism/modules/order/application/OrderCenterServiceTest.java:190` covers page-size/sort clamping. The residual list response still maps every row through full `toResponse()`, expanding `items`, `paymentTransactions`, `refunds`, `vouchers`, and `invoices` at `OrderCenterService.java:1585` through `1611`.
- Residual risk: Large orders in a page can still trigger avoidable lazy loads, serialization, and oversized payloads even though the top-level order count is now capped.
- Suggested fix: Use a list-specific summary DTO/projection and reserve child collections for the order detail endpoint with an explicit fetch graph.

### CT-BE-028 - Medium - Author shared-route deletion does not delete child rows

- Status: Open.
- Evidence: `backend/src/main/java/com/tibet/tourism/modules/community/application/SharedRouteService.java:121` through `130` authorizes an owner delete and then calls `routeRepository.delete(route)` directly. The initial schema keeps `route_comments.route_id` and `route_likes.route_id` foreign keys without cascade at `backend/src/main/resources/db/migration/V1__initial_core_schema.sql:222` and `233`. The admin delete path has a safer comparison helper at `backend/src/main/java/com/tibet/tourism/modules/admin/web/AdminCommunityController.java:334` through `338` that deletes likes and comments first.
- Residual risk: A normal author cannot delete their own shared route once it has comments or likes because child rows can block the parent delete.
- Suggested fix: Reuse the child-cleanup delete path in `SharedRouteService.deleteRoute(...)`, or add explicit cascade/orphan cleanup with a regression test for author deletion after comments/likes exist.

### CT-FE-009 - Low - Admin/frontend types still rely on broad `any`

- Status: Open.
- Evidence: `frontend/src/views/AdminDashboard.vue:1111` still uses `any[]` for stats payloads, helpers accept `route: any` around `AdminDashboard.vue:1321`, and admin state still uses `ref<any[]>` / `ref<any>` for spots, news, carousels, routes, hotels, and room types around `AdminDashboard.vue:1391`, `1408`, `1884`, `1934`, `2037`, and `2090`. `frontend/src/views/UserProfile.vue:30` through `36` and `frontend/src/views/HotelBooking.vue:340` through `341` still use broad `ref<any>` / `ref<any[]>` for profile, booking, hotel, and room-type state.
- Suggested fix: Add typed admin DTOs and `PageResponse<T>` in `frontend/src/api/types.ts` or derive them from generated schema types, then use typed refs and typed `api.get<T>()`/partial update payloads.

### CT-FE-011 - Medium - Major news/route/order/booking flows still bypass i18n

- Status: Open.
- Evidence: `frontend/src/views/News.vue:300` still hardcodes `zh-CN` date formatting. `frontend/src/views/RoutePlanner.vue` still has user-visible Chinese and `zh-CN` formatting outside `t(...)`, including representative lines `72`, `75`, `284`, `315`, `318`, `335`, `796`, `1365`, `1701`, `1707`, `1774`, `1988`, `2129`, `2173`, and `2201`. `frontend/src/views/OrderCenter.vue` has the same issue for fallback labels, metrics, date/currency formatting, and item labels around lines `152`, `200`, `491`, `594`, and `605`. `frontend/src/views/HotelBooking.vue:105`, `259` through `262`, `291`, `466`, `489`, `499`, `519`, and `524` contain hardcoded booking copy/status text outside i18n.
- Suggested fix: Move labels/messages/ARIA strings into `zh.json` and `bo.json`, and drive currency/date formatting from the active locale instead of hardcoded `zh-CN`.

### CT-FE-017 - Low - User profile tablist ARIA pattern is incomplete

- Status: Open.
- Evidence: `frontend/src/views/UserProfile.vue:811` through `818` declares `role="tablist"` / `role="tab"` but lacks roving `tabindex`, keyboard navigation, and `aria-controls`; tab content at `UserProfile.vue:859`, `938`, `1007`, and `1080` is rendered as ordinary `div` content rather than labelled `tabpanel`s.
- Suggested fix: Mirror the complete tab pattern already used in `frontend/src/views/RouteCommunity.vue`, including `aria-controls`, `tabpanel`, `aria-labelledby`, roving focus, Arrow/Home/End handlers, and hidden inactive panels.

### CT-FE-018 - Low - Some admin data-load failures are silently rendered as empty lists

- Status: Open.
- Evidence: `frontend/src/views/AdminDashboard.vue:1894` catches carousel load errors and sets an empty list, `AdminDashboard.vue:1967` does the same for routes, `AdminDashboard.vue:2049` for hotels, and `AdminDashboard.vue:2104` for room types. Unlike other admin loaders in the same file, these paths do not log a sanitized error, set an error state, or show a toast.
- Suggested fix: Use the existing `safeClientErrorMessage` / `summarizeClientError` and toast/error-state patterns so failed admin APIs are not indistinguishable from genuinely empty data.

### CT-FE-023 - Medium - Account/order pages hide data beyond page 0

- Status: Open.
- Evidence: Backend account/order endpoints now page results, but several frontend consumers still keep only the first response page. `frontend/src/views/OrderCenter.vue:495` through `497` calls `/api/orders/my` without page state and assigns only the returned array. `frontend/src/views/HotelOrders.vue:141` through `143` and `frontend/src/views/UserProfile.vue:238` through `241` take only `response.data.content` from `/api/hotel-bookings/my`. `frontend/src/views/UserProfile.vue:278` through `282` reads `/api/auth/me/comments` into arrays without preserving page metadata or load-more controls.
- Residual risk: Users with more than the backend default page of orders, hotel bookings, or profile comments cannot reach older records from these views.
- Suggested fix: Add shared paged-list state/load-more controls for account orders, hotel bookings, and profile comments, or request explicitly bounded summary endpoints when only a recent slice is intended.

### CT-FE-024 - Low - Image upload field has a hardcoded default admin endpoint

- Status: Open.
- Evidence: `frontend/src/components/ImageUploadField.vue:34` defaults `uploadEndpoint` to the literal `/admin/upload-image` even though endpoint paths are meant to be centralized and `frontend/src/api/endpoints.ts:181` exposes `endpoints.admin.uploadImage`. Current callers pass the registry value explicitly, but the component default remains a hidden bypass.
- Residual risk: A future caller can omit the prop and silently reintroduce a hardcoded endpoint path outside the registry guardrails.
- Suggested fix: Make `uploadEndpoint` required, or import the endpoint registry default and add a source guardrail test for the component default.

### CT-OPS-007 - Low - Nginx may overwrite client forwarding chain

- Status: Needs production topology confirmation.
- Evidence: `docker-compose.prod.yml:46` defaults `TRUST_PROXY_HEADERS=true`, `docker-compose.prod.yml:47` trusts broad private CIDRs, backend trusted-proxy logic reads `X-Forwarded-For`, and `frontend/nginx.conf:96`, `119`, `160`, `220`, and `265` set `X-Forwarded-For` to `$remote_addr`.
- Suggested fix: If there is an upstream CDN or load balancer, configure `real_ip_header`/trusted upstreams, use `$proxy_add_x_forwarded_for`, and narrow `TRUSTED_PROXY_CIDRS` to the actual proxy networks.

### CT-OPS-008 - Low - Supply-chain inputs are not fully pinned

- Status: Needs release policy confirmation.
- Evidence: `backend/Dockerfile:2`, `backend/Dockerfile:9`, `frontend/Dockerfile:2`, `frontend/Dockerfile:24`, and `scrapler/Dockerfile:1` use mutable image tags; `scrapler/requirements.txt:1` and other entries use version ranges; `.github/workflows/ci.yml:19` and other workflow steps use action tags; `frontend/Dockerfile:5` and `.github/workflows/ci.yml:208` still allow `npm install` when no lock is present.
- Suggested fix: For production releases, pin image digests and GitHub Actions SHAs, add locked or hashed Python dependency inputs, and fail builds when frontend lockfiles are missing.

### CT-OPS-018 - Medium - reCAPTCHA verification sends secrets in URL query parameters

- Status: Open.
- Evidence: `backend/src/main/java/com/tibet/tourism/common/security/antibot/RecaptchaService.java:43` through `48` builds the verification POST URI with `secret`, `response`, and `remoteip` as query parameters.
- Residual risk: Outbound proxy, APM, gateway, or access logs can capture the reCAPTCHA secret, user token, and client IP from request URLs even though the call uses POST.
- Suggested fix: Send `secret`, `response`, and `remoteip` as an `application/x-www-form-urlencoded` request body, and add a focused test that captures the outbound URI/body shape without exposing raw secrets in logs.
