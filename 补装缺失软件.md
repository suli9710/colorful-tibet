# 补装缺失的软件

## 🔍 问题

检测到以下软件未安装：
- ❌ Node.js
- ❌ npm
- ❌ Nginx

## ✅ 快速安装

在服务器终端执行以下命令：

### 1. 安装Node.js 18

```bash
# 添加NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# 安装Node.js
sudo apt install -y nodejs

# 验证安装
node -v
npm -v
```

### 2. 安装Nginx

```bash
# 安装Nginx
sudo apt install -y nginx

# 启动Nginx
sudo systemctl start nginx

# 设置开机自启
sudo systemctl enable nginx

# 验证安装
nginx -v
sudo systemctl status nginx
```

### 3. 安装PM2（可选，用于进程管理）

```bash
sudo npm install -g pm2
```

### 4. 安装Certbot（SSL证书工具，可选）

```bash
sudo apt install -y certbot python3-certbot-nginx
```

## 📋 一键安装所有缺失软件

复制以下命令一次性安装：

```bash
# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装Nginx
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 安装PM2和Certbot
sudo npm install -g pm2
sudo apt install -y certbot python3-certbot-nginx

# 验证安装
echo "=== 验证安装 ==="
node -v
npm -v
nginx -v
systemctl status nginx
```

## ✅ 验证安装

执行以下命令检查：

```bash
# 检查Node.js
node -v
# 应该显示：v18.x.x

# 检查npm
npm -v
# 应该显示版本号

# 检查Nginx
nginx -v
# 应该显示：nginx version: 1.x.x

# 检查Nginx服务
sudo systemctl status nginx
# 应该显示：active (running)
```

## 🎯 安装完成后

安装完成后，继续执行 `环境安装完成后的步骤.md` 中的后续步骤：
1. 上传项目代码
2. 配置后端
3. 编译启动后端
4. 构建前端
5. 配置Nginx

## ⚠️ 如果安装失败

### Node.js安装失败
```bash
# 更新软件包列表
sudo apt update

# 再次尝试安装
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```

### Nginx安装失败
```bash
# 更新软件包列表
sudo apt update

# 再次尝试安装
sudo apt install -y nginx
```

---

**执行上面的命令后，所有必需的软件就都安装完成了！**


