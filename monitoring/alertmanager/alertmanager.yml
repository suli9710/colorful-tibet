route:
  receiver: webhook
  group_by:
    - alertname
    - severity
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: webhook
    webhook_configs:
      - url: https://example.com/alerts
        send_resolved: true
